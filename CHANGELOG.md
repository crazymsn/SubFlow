# 更新记录

当前源码版本 **1.3.65（本地修复，尚未发布）**。公开下载见 [最新 Release](https://github.com/crazymsn/SubFlow/releases/latest)，安装说明见 [文档索引](docs/README.md)。以下历史记录保留用于追溯，不表示旧安装包仍可下载。

## 1.3.65 — 旧式钥匙串静默读取

- 兼容旧式 macOS 文件钥匙串：查询期间在当前进程禁止认证交互，并恢复原状态；互斥锁避免与主动保存、清除密钥并发冲突。禁止交互设置失败时直接停止查询。
- Intel 实测确认仅设置单次查询参数仍会出现 ACL 授权；补充保护后，原本要求认证的条目在 1 秒内返回不可读取，不再等待密码。

## 1.3.64 — Mac 启动与首次组件检查（验收候选，未交付）

- macOS 读取 API Key 时禁止弹出钥匙串认证窗口；需要认证的旧凭据保持原状，由用户在客户端填入 API Key 后使用。启动读取不再自动迁移、修改或删除旧凭据。
- macOS 内置安装器首次版本检查允许等待 60 秒，兼容新安装包及 Rosetta 的首次启动检查；保留取消和超时后的进程清理。

## 1.3.63 — Mac 在线视频下载修复

- Mac 打包内置 Node.js 和匹配的 YouTube EJS 脚本，下载组件优先使用应用内运行时，不依赖终端环境变量。
- 将内置 FFmpeg 的实际位置传给 yt-dlp，修复 Finder 启动后无法合并视频、音频的问题。
- 下载错误提示使用当前系统的 Cookies 路径，区分组件缺失和登录验证；补充 Mac 文本编辑的纯文本保存步骤。

## 1.3.62 — Mac 图标、Cookies 与 DMG

- 保留原始云形标识，重新制作真正透明的 macOS 圆角方形图标；16/32 像素及 Retina 对应尺寸采用简化标识，提供完整 ICNS 尺寸集。
- 正确识别 Netscape 文件中 `#HttpOnly_` 前缀的登录 Cookie，避免有效的 YouTube/Bilibili 登录态被忽略；补充 Mac 路径、导出及安装说明。
- 增加 Apple M / Intel 分架构 DMG 构建工具，提供 Applications 拖拽安装入口；发布前核对架构、模型清单和签名，并挂载只读 DMG 执行真实客户端自检。
- 允许明确指定的 Intel CPU Rosetta 构建；继续拒绝将 Rosetta 环境误标为 Apple M 原生构建。媒体工具必须与目标架构匹配。

## 1.3.61 — Apple M 实机修复

- Mac 客户端采用本次提供的 Sub.png 图标，生成多尺寸 ICNS，并同步 Dock / 窗口图标。
- 在原生环境初始化前关闭 ONNX Runtime 遥测，避免后台上传线程在退出时发生原生崩溃。

- Qwen 官方音色在 MPS 推理时使用 CPU 随机抽样，保留原有温度和 top-k 分布，修复实测出现的开头多余音节；状态接口分别报告模型和抽样设备。
- Qwen 在 MPS 使用兼容的注意力计算，避免 Torch 2.5 分组注意力触发原生进程崩溃。
- Qwen 和 GPT-SoVITS 的长音频卷积按完整感受野分段计算；转置卷积保留重叠贡献与偏置，解决旧 macOS 的 65536 输出维度限制。CPU / CUDA 保持原计算路径。
- Mac 分卷包保留框架符号链接；用 `-snld20` 还原父目录及间接链接，检查链接全部位于应用内；发布流程在实际解压的应用上重新验证签名并执行自检。
- 自检从完整离线清单定位 GPT-SoVITS；失败时等待后台线程退出并保留错误报告，禁止打包应用借用构建目录通过检查。
- 翻译客户端读取 macOS 系统代理，保留显式环境代理与绕过规则；本机回环媒体请求不再误送给代理。
- Mac 构建支持指定 Python、隔离环境和输出目录；复用离线模型时刷新当前源码与清单校验值。组件验收覆盖真实长音频算子，并保留冷启动超时日志。

## 1.3.60 — 完整配音包与界面修复

- 翻译失败可在任务栏或错误弹窗继续，读取修正后的令牌、模型和润色设置；重试前获取锁失败时保留入口。恢复仅放宽翻译选项，继续校验源视频与识别缓存。
- 修复 Mac 小窗口下硬件型号文字被裁切，补齐干净构建环境的测试依赖与空间准备。
- 去掉“深度云创科技”获得焦点时的橙色边框；保留超链接和键盘激活，所有交互状态均无边框。
- 配音准备阶段移除固定的“首次使用需下载模型”提示；内置模型的校验、加载与实际下载信息仍按真实运行过程记录。

## 1.3.59（本地，尚未发布）— 顶栏、配音时长与翻译恢复

- 顶栏主题与语言下拉框等宽，和 GitHub 按钮统一为 40 px 高；文字放大至 16 px，统一边框与圆角。
- 分区编号与标题行垂直居中，说明文字放在标题下方，不再带偏编号。
- 按真实合成时长规划整段配音，借用停顿并限制台词起点偏移在 600 ms 内；不得重叠、截掉片尾或拉长短句。密集台词保音高加速，不再因超过 1.25 倍而中断整片。
- 翻译字符预算仅用于提示模型，不再硬截断返回文字；更新翻译缓存键以避开旧截断结果。
- 配音稿润色按最多八句分批，格式异常拆批重试；可选润色暂时失败时保留已完成的目标语言译文。
- WhisperX 显存不足先减小识别批次；GPU 无法完成识别时再尝试 CPU。网络和模型资源错误保留原始错误，不当作显卡故障。

## 1.3.58（本地，尚未发布）— 公司标题字号

- 将顶栏中央“深度云创科技”放大至 24 px，与产品标题一致，保持居中及无悬浮效果。

## 1.3.57（本地，尚未发布）— 顶栏与硬件型号显示

- 将“SubFlow 语幕”字号从 20 px 调整为 24 px，保持标题字体一致。
- “深度云创科技”取消悬停变色、下划线和提示气泡，保留超链接及键盘焦点提示。
- 待机硬件型号使用简写，例如 RTX 3060 Laptop、M1、M4 Pro；保留型号变体，不改变设备检测与 GPU 调度。

## 1.3.56（本地，尚未发布）— 品牌与待机任务状态

- 恢复左侧“SubFlow 语幕”和窗口标题；“深度云创科技”独立居中，保留导航站链接。
- 待机时恢复任务标题、等待状态、百分比及进度条，仅在日志框内显示硬件检测与型号；开始任务后显示正常日志。
- 读取 NVIDIA 显卡名称及实际 Apple M 芯片型号；不支持 CUDA 的 Windows 显卡显示型号并注明 CPU 处理，不改变推理设备选择。

## 1.3.55（本地，尚未发布）— Mac 验收与线程退出

- Mac 实机手册覆盖 M1 / Intel、四套运行环境、三种配音模式、八语种、真实设备、翻译恢复及证据记录。
- 随完整包提供只读离线组件检查工具；组件通过与实际产品验收分开记录。
- Apple M 完整包强制使用 MPS/CPU 原生依赖；Mac 构建使用隔离 venv，拒绝 Rosetta 与架构混用。
- 明确要求 GPU 的真实配音验收不再接受静默 CPU 回退；补全通用 Mach-O 64 位容器的签名识别。
- 后台任务结束时等待原生线程完全退出，再延迟释放 Qt 对象；移除 WhisperX 未使用的旧解释器辅助函数。
- 修复试听测试遗留 Qt 延迟释放事件导致后续任务恢复测试崩溃的问题；会话保留统一 QApplication，播放器测试退出前回收延迟对象。
- 复用完整包时检查系统、架构及四套运行环境，拒绝把旧版缺少识别环境的包或异构包当成新完整包。

## 1.3.54（本地，尚未发布）— 识别环境与翻译恢复

- 完整包构建纳入 Whisper / WhisperX 独立识别环境，保留 Windows GPU / CPU 与 Mac 原生架构依赖选择。
- 修复 WhisperX 冷启动超过 12 秒被误判的问题；复用已验证解释器，识别完成后释放模型再加载对齐模型。
- 修复视频任务覆盖所选 Qwen 引擎和 SubFlow 设计音色的问题。
- 顶栏显示可点击的“深度云创科技”，待机任务面板仅显示 GPU 检测结果。
- “继续”支持从失败或缺译的翻译阶段重试，复用原识别结果并校验缓存完整性。

## 1.3.53（本地，尚未发布）— 多语种设计音色与常显设置

- 核查 Qwen 官方开源发布，保留可离线部署的 Qwen3-TTS。
- 在 9 个官方预设之外内置 14 个独立设计音色，七种口语各有男女声，覆盖八个字幕目标语种。
- 西班牙语、法语、德语、俄语自动匹配对应设计女声；菜单标明音色来源及性别，保留手动选择。
- 设计音色复用已有 Base 模型；与 CustomVoice 切换时释放旧模型，保留 GPU 优先与 CPU 回退。
- 语音识别与翻译始终展开，删除旧折叠按钮、交互逻辑与失效样式。
- 清理旧构建、被完整包替代的 GPT-SoVITS 副本、未使用的 v1/v3/v4/v2Pro 权重及测试产物；保留当前三个配音模式的模型与环境。

## 1.3.52（本地，尚未发布）— 完整配音包

- 桌面构建默认携带 Qwen 标准音色、Qwen 原声克隆和 GPT-SoVITS v2 的模型及可迁移 Python 环境。
- 优先随包模型，按内容校验，不因解压改变时间戳而重新下载；损坏完整包明确提示重新解压。
- Windows CUDA 环境支持无显卡设备直接使用 CPU；Mac 按原生架构打包 MPS / CPU 环境。
- GPT-SoVITS 字典缓存改放用户目录，避免应用目录只读时合成失败。
- 补齐 NLTK ZIP 兼容资源与韩语词典，移除韩语推理时自动调用 pip；普通资源错误不再触发错误的 GPU→CPU 回退。
- 发布流程增加空白配置、禁止外网的三引擎真实 CPU 合成检查，并改用适合大文件的分卷归档。

## 1.3.51（本地，尚未发布）— 桌面工作区改版

- 将视频素材、字幕与语言、配音与音色分为三个独立区域，识别与翻译设置单独折叠；配音和试听不再隐藏在更多选项中。
- 宽窗口右侧固定任务进度与日志，窄窗口移到工作区下方；底部输出路径与操作按钮固定，支持最低 960×640 的逻辑窗口尺寸。
- 统一深浅主题的间距、卡片、输入框和焦点样式，提高浅色主题进度数字的对比度。
- 修复滚轮误改语种与音色、滚动定位只显示光标而裁切输入框、长文件名溢出、切换界面语言重置任务状态，以及切换引擎残留旧 GPU 状态的问题。
- 上传、开始处理、下载与复选框支持键盘操作；缺少令牌或模型时自动展开设置并定位输入框。八种界面语言同步更新。
- 保留 Qwen 全部 9 种音色、CUDA / MPS / CPU 自动选择和已有字幕配音规则。新版使用方式见 [工作区指南](docs/workspace.md)。

## 1.3.50（本地，尚未发布）— 完整音色与设备状态

- 开放 Qwen CustomVoice 全部 9 种标准音色（5 男声、4 女声），菜单标注性别和原生语种；目标语言只影响排序，不限制可选音色。
- 切换目标语言与界面语言时保留手动音色选择，试听与正式配音传递真实说话人 ID；自动选择继续按目标语言匹配。
- Qwen 服务状态显示实际 CUDA / MPS / CPU 设备。修复自动设备环境变量为空或含空格时错误选择 CPU 的问题，验证 GPU 模型加载和 CPU 回退。
- 使用说明见 [Qwen 音色与设备](docs/voices.md)。

## 1.3.49（本地，尚未发布）— 标准音色与单行字幕

- 全部界面目标语种默认采用 Qwen3-TTS CustomVoice 标准音色，无需参考音频；英文可选 Aiden / Ryan，日语采用 Ono_Anna，中文使用中文预设。保留 Qwen 原声克隆及 GPT-SoVITS 选项。
- 标准音色不继承旧参考音频或逐字稿，避免失效临时文件、错误提示文本和中文跨语种克隆影响默认旁白。试听、正式任务、设备选择与缓存使用同一引擎身份。
- 每种语言最多一行，字号及上下位置保持固定；长译文均衡分页，较短一侧保留完整上下文，避免硬切短译文和缺失一侧时位置跳动。
- 修复切换界面语言丢失音色、标准音色服务状态检测、显式 Qwen 试听地址被默认值覆盖的问题。
- 保持连续语句合成和自然语速限制；更新处理缓存版本。详细速度对比与实际验收见 [质量记录](docs/quality-qa-1.3.49.md)。

## 1.3.48（本地，尚未发布）— 字号与配音质量

- 字幕使用固定字号，长句按宽度分页显示；取消逐句缩字，避免字体忽大忽小。
- 配音按连贯语句合成，与字幕分页分离；翻译后按可用时长整理口语化配音稿，字幕同步采用整理后的译文。
- 短语音保持自然语速；仅允许最高 1.25 倍的适度加速，过密台词明确报错，避免极端变速损坏语音。
- 自动参考音频在完整对应识别时间段时使用匹配原文；固定采样参数，减少逐片段语调不稳定。
- 中文参考音色转英文优先采用内置 Qwen3-TTS，改善跨语种英文发音；GPT-SoVITS 保留用于其他原有语言组合。
- 处理与音频缓存更新版本，旧的强制变速音频不会作为新成片复用。

## 1.3.47（本地，尚未发布）— 恢复完整配音链路

- 恢复配音界面、GPT-SoVITS / Qwen3-TTS、本地自动安装、CLI 和桌面 / Docker 打包入口。
- 自动提取的参考音频不再误用保存的问候语作为逐字稿；显式留空的逐字稿不会被环境变量覆盖。
- 配音任务全程持有引擎使用锁，避免试听或启动其他引擎在句间卸载当前模型。
- 失效参考音频在加载模型前报错；修复短视频被默认起始偏移截短、直接指定 Qwen 服务地址被覆盖的问题。

### GPU 调度、试听与错误提示

- 修复配音阶段固定停留 94%：按实际句数报告合成、对齐、校验和混音进度，耗时步骤持续显示计时，取消后停止更新。
- 自动使用本地 Qwen3-TTS 补齐西班牙语、俄语、法语、德语配音及试听；保留参考音色。首次下载约 2.5 GB 模型并校验固定版本，依赖隔离安装，后续离线复用。
- 多语种模型优先 CUDA / Apple MPS，GPU 资源或算子失败时回退 CPU；切换引擎释放本客户端空闲模型，避免同时占用显存。修复上游生成接口未传递取消信号的问题。
- 配音面板默认填写“您好，请问有什么能帮您？”及目标语种译文，切换目标语种自动更换试听内容；支持编辑试听文本，并与参考音频逐字稿分开处理。
- Windows / Linux x64 自动检测 NVIDIA 驱动并准备 CUDA 推理环境，避免内置 CPU PyTorch 遮蔽显卡；Apple Silicon 继续优先 MPS，无可用 GPU 时使用 CPU。
- 自动 GPU 识别与配音优先选择对应的受管理环境；CUDA 配音复用已有模型。标准 Whisper 与非流式配音的 GPU 失败支持 CPU 重试。
- 隔离 pytest 和冻结客户端自检的用户目录、配置与凭据，避免测试污染日常使用环境。
- 自动忽略旧测试遗留的失效临时参考音频及配套提示文本；用户自行选择的参考音频不会被静默替换。
- GPT-SoVITS 返回异常类型和具体原因，补充损坏音频无法解码、生成器未返回音频的诊断，不再只显示 `tts failed`。
- 错误弹窗支持深浅主题、处理建议、展开详情、复制脱敏错误和重新选择参考音频；补齐八种界面语言并修复按钮对比度。

## 1.3.46 — 2026-09-06

- 中文源片选择简体 / 繁体时保留原声；字幕时间限制在媒体范围内，长句保留完整时间并按词、静音或文本边界拆分。
- 配置保存支持原子替换和跨进程锁，防止中断或多窗口更新丢失配置。
- 清理停用 TTS 模块与真实视频测试数据，保留合成回归用例。
- Docker Compose 默认拉取版本镜像，完善设置、模型与语言缓存持久化、进程退出及日志轮转；源码构建使用独立覆盖文件。
- Docker 采用多阶段构建和缓存，原生验证 amd64 / arm64，全部检查通过后发布 Docker Hub 多架构标签。

## 1.3.34 — 2026-09-06

- FFmpeg stderr 持续读取并仅保留末尾 64 KiB，避免重复诊断随运行时间不断占用内存；stdout 临时落盘后完整返回，保留 FFprobe 等结构化结果。
- 统一管理 FFmpeg 与子孙进程，正常退出、非零退出、取消和读取回调失败后均清理进程树，再结束诊断读取线程。
- 静音检测改为逐行解析，保留长视频的全部静音事件，不依赖诊断尾部缓存；忽略无效、非有限及逆序时间区间。

## 1.3.33 — 2026-09-06

- 默认自动 MPS 识别每次任务前核验受管理的原生环境，并使用外部识别进程；主程序中恰好可导入 Whisper 不再绕过环境选择。
- 显式设置 SUBFLOW_PYTHON / SUBFLOW_WHISPER_PYTHON 优先于主程序环境，无效指定明确报错；空白主变量允许使用备用变量，路径展开并转为绝对路径。
- 仅 Whisper 模块导入失败时尝试其他运行环境；推理开始后的 ImportError 向调用方传播，避免在其他环境重复执行并掩盖原错误。
- 修复多段配音混合后时间戳异常导致 FFmpeg 持续编码或尾部静音缺失的问题；最终音轨按采样数补齐、裁剪并重建时间戳。更新处理修订，旧任务需重新处理。
- FFmpeg 失败异常保留末尾 8192 字符，避免重复警告把界面和测试报告扩展为数百 MB。

## 1.3.32 — 2026-09-06

- 默认自动 MPS 配音启动仅使用项目随附或缓存的适配源码，以及受管理的 MPS 解释器，避免旧 venv、其他整合包或系统 Python 覆盖自动环境选择。
- 新建 MPS 服务前核验并按需修复缓存环境；失败时停止启动。运行环境探测拒绝 Intel 架构解释器，即使该 PyTorch 声明编译了 MPS 支持。
- 保留显式指定配音目录或解释器、关闭自动安装及 CPU 模式下的手动整合包兼容行为。

## 1.3.31 — 2026-09-06

- 四个旧 PowerShell 准备入口统一调用客户端的 managed 安装流程，沿用平台默认 CPU / MPS 及显式设备设置，不再各自安装未固定版本的依赖。
- 源码准备使用项目随附的适配版本；模型下载复用资源校验和安装锁，不再克隆上游最新源码或仅按目录非空判断资源完成。
- Python 准备命令增加 `--backend`，覆盖仅在本次调用有效；主程序 Python 3.13 可准备独立的 Python 3.11 推理环境。安装失败向 PowerShell 调用者传播。
- 同步安装与错误提示，明确 managed 缓存位置、兼容脚本行为，以及社区包首次联网准备的要求。

## 1.3.30 — 2026-09-06

- 源码运行与客户端打包共用安装器查找逻辑，优先使用当前 Python 环境的 uv，并验证项目固定版本 0.11.8，避免系统 PATH 中旧版安装器破坏自动安装。
- 打包客户端仅使用内置位置的安装器；缺失或版本错误时明确提示重新下载完整客户端，不再误用当前目录或系统中的其他 uv。
- 安装器版本检查支持取消和超时，退出后回收子孙进程；新增真实进程回归检查。

## 1.3.29 — 2026-09-06

- CPU / CUDA 环境的两阶段安装与修复均固定 PyTorch 后端及完整构建版本，避免通用依赖重装将 CUDA 包替换为其他构建。
- 环境探测核对 torch / torchaudio 构建、CUDA 版本和 MPS 编译支持；健康旧环境只迁移准备标记，检查失败才修复。
- 每个运行环境独立保存约束文件，避免不同安装锁下相互覆盖；应用设备选择优先于继承的 UV_TORCH_BACKEND 设置。

## 1.3.28 — 2026-09-06

- 完成报告与最终任务状态统一暂存和提交，后续文件替换失败时回滚已提交记录，首次失败也不遗留成功报告。
- 最终记录准备期间仍响应取消；开始替换后完整提交或回滚，避免同时出现“完成”和“已停止”的矛盾状态。
- 自动重新导出被取消时记录停止状态，保留阶段产物供恢复；最近任务指针在完成记录提交前保存。

## 1.3.27 — 2026-09-06

- 恢复任务时根据已核验字幕重新生成外部 SRT / ASS，避免将原位置或新位置已有的无关字幕写入成功报告。
- 缺失字幕的重建也核对原渲染设置及 ASS 内容摘要，禁止绕过恢复检查并混用不同样式的字幕与成片。
- 外部 SRT、ASS 和工作目录中的烧录 ASS 统一暂存，任一文件提交失败时回滚已提交文件。处理修订更新，旧任务需重新处理。
- 自动重新导出在报告与任务状态持久化成功后才发送完成事件，避免保存失败前已经显示完成。

## 1.3.26 — 2026-09-06

- 导出复制在读取数据时计算 SHA-256，并与任务记录对比后再替换目标，防止缓存校验后被同大小、同时间戳文件替换。
- 原声烧录、带配音成片、配音旁路文件和已有成片重新导出均传递已核验的内容摘要；同路径及硬链接别名也验证记录。
- 内容不匹配、取消和 I/O 错误保留原目标文件。处理修订更新，旧任务需重新处理，避免复用旧逻辑可能记录错误的导出结果。

## 1.3.25 — 2026-09-06

- Mac 试听使用受客户端管理的 QProcess；停止及退出时终止并回收播放器，包括仍在启动中的进程，播放器异常退出会显示错误。
- Qt 试听等待异步加载及实际播放状态，正确处理结束、解码失败和等待超时；释放旧播放器、音频输出及计时器，忽略过期事件。
- 仅在播放器报告开始后显示“正在试听”；播放期间保持按钮忙碌状态，系统播放器无法打开文件时明确报错。
- 试听返回时核对完整参考设置与所选视频，合成结束后再次核验源视频内容；过期结果和旧任务错误不会覆盖当前状态。

## 1.3.24 — 2026-09-06

- 修复正满幅音频转为 PCM16 时翻转成负峰值的问题；统一归一化、舍入及饱和转换，拒绝空音频、非单声道及 NaN / 无穷值。
- 后处理不再原地修改输入波形；分段停顿使用实际输出采样率，修正 24 kHz 等输出中的停顿时长。超采样成功和缺失模型分支使用同一转换。
- OGG 编码线程错误向调用方传播，恢复线程栈配置；AAC 检查退出码和空输出，超时后终止并回收编码进程，不返回伪成功。
- 新增无需神经网络权重的后处理与四种编码格式检查，接入 Windows、两种 Mac 架构及 Docker 构建。更新语音缓存修订，旧 PCM 需要重新生成。

## 1.3.23 — 2026-09-06

- 成功加载的模型状态携带独立版本标识；失败加载保持原标识。试听、逐句配音和成品恢复校验该标识，防止换模型后误用旧缓存。
- 非流式合成在模型锁内校验请求版本，响应头标明实际生成版本；排队期间改变模型或语言支持时重新校验。
- 模型变化时整次试听 / 配音最多自动重试一次，支持 CPU 回退后的重新合成。混音结束后再次核验，失败时保留原成片。
- 外部旧服务仍可合成，但没有模型版本时不复用跨任务语音缓存或已完成配音缓存。服务重启也产生新版本，需重新配音；中文同语种原声任务不查询配音模型。

## 1.3.22 — 2026-09-06

- 权重切换、模型恢复和设备 / 精度变更先加载候选状态，成功保存配置后再提交；缺失权重、后期加载异常或配置写入失败时保留原模型及缓存。
- 配置使用 UTF-8 暂存、同步并原子替换；嵌套加载仅提交一次，`save=False` 不修改磁盘配置。
- CPU/MPS 回退失败不再提前改坏当前设备状态；规范化 CPU/MPS 设备别名。
- BERT / HuBERT 路径随成功加载更新；SoVITS 版本变化时刷新静音令牌相似度，避免旧版本向量和无用梯度历史。
- 释放新模型不再需要的声码器 / 说话人编码器；语言校验读取当前模型配置。更新任务及语音缓存修订。

## 1.3.21 — 2026-09-06

- 在执行模型前校验批大小、采样参数、语速、分段参数和随机种子，拒绝零批大小、非法范围及非有限数值，避免循环卡住或算术错误。
- 无有效文本或没有可配音片段时明确报错；停止合成不再返回一秒静音，不会因这些状态重载模型。
- 流式接口预取第一个音频块，预处理失败在发送成功响应之前返回错误；预取后立即断开也能关闭生成器并释放锁。
- 修复 `top_k=0` 在并行采样中的空索引错误，保持关闭 top-k 过滤的语义。
- 升级任务和语音缓存修订，避免复用旧版本错误返回的静音结果。

## 1.3.20 — 2026-09-06

- GPT-SoVITS 参考特征缓存绑定完整音频内容；同一路径替换文件后重新提取。辅助参考的内容、顺序和删除也会刷新缓存。
- 参考预处理失败或处理中内容变化时恢复完整旧缓存；辅助参考不再覆盖主参考的原始波形字段。缺失辅助文件明确报错。
- 提示特征缓存包含语言和模型版本；空白提示不再索引越界。权重、设备、精度变化会清除旧特征，重载 BERT 同步更新文本预处理器。
- 明确 GET 辅助参考音频为查询参数，修复新版 FastAPI 在客户端 CI 中错误识别为表单字段的问题。
- 更新任务与配音缓存修订，旧版本任务需重新处理，避免继续使用旧参考缓存生成的音频。

## 1.3.19 — 2026-09-06

- GPT-SoVITS 推理在工作线程执行，避免 CPU 合成阻塞 HTTP 健康检查。服务端统一串行化合成、参考音频和模型修改。
- 流式回复持有模型至结束或关闭；请求取消时等待当前推理线程结束再释放，避免后续请求交叉修改模型。明确关闭推理生成器。
- 缺少 GET 语言参数返回输入错误；POST 使用 Pydantic 2 的序列化接口。
- 配音请求默认等待 1800 秒，可用 `SUBFLOW_GPTSOVITS_TIMEOUT` 调整；超时提示与服务连接失败区分。配音输出使用独立暂存文件，禁止覆盖参考音频。

## 1.3.18 — 2026-09-06

- 成品另存将视频、字幕和报告一起准备、提交；后续文件替换失败时回滚前面的文件。回滚失败则保留可恢复备份并报告位置。
- 报告保存各成品的 SHA-256；另存校验任务编号、完成状态、源视频及成品内容，同一目标路径也不能绕过校验。旧报告缺少校验记录时需先重新导出或处理。
- 独立下载预留目标视频与诊断日志的文件占用，避免覆盖其他任务正在读取的视频。保存诊断日志失败不再掩盖原始停止或下载错误。
- POSIX 进程组清理遇到权限错误时检查是否仍有存活成员；仅已消失或只剩僵尸的组可忽略该退出竞态，仍有存活进程时继续报告错误。

## 1.3.17 — 2026-09-06

- 试听缓存绑定语言、音色、端点、参考内容、提示文本和试听台词；指定同一路径后更改请求也会重新合成，损坏或无记录音频不复用。
- 自动参考音频绑定源视频完整内容及截取区间；提取失败、过短或取消时保留旧参考文件。试听后台任务登记文件占用，防止并发覆盖。
- 从 done 恢复时核验并导出已完成成片；非配音任务也保留内部烧录视频，外部输出被删除或替换后可以正确恢复。
- 阶段完成记录保存字幕 / 烧录 / 配音设置，拒绝将旧媒体当作新设置的结果；恢复保留翻译统计与缺译记录。
- 已完成配音在导出失败后可从 done 重新导出，无需再次合成。处理修订更新后，旧版本任务需重新处理建立新的阶段记录。

## 1.3.16 — 2026-09-06

- 逐句配音缓存保存请求身份及完整音频摘要；缺少记录、音频被替换或内容改变时重新生成，不再仅凭文件存在复用。
- 拟合缓存绑定实际原始 WAV 的内容；原始音频重新合成后不会搭配旧拟合音频。混音前再次校验，参考音频或设置在合成途中变化时拒绝提交。
- 分块验证 PCM WAV 完整性，拒绝空音频、截断音频和非法目标时长；拟合使用独立暂存文件，失败保留旧结果。
- 最终配音视频暂存成功后再替换，合成 / 混音错误和取消不会截断旧成片；禁止覆盖视频或音频输入。
- 更新处理修订，旧任务需重新处理；新格式逐句缓存继续支持失败后的复用。

## 1.3.15 — 2026-09-06

- 将阶段产物的 SHA-256 随完成记录原子保存；恢复前校验转写、字幕、音频及内部成片，合法但被替换的缓存也不能继续使用。
- 自动导出发现所需缓存改变时重新处理；手动恢复指出需要重跑的阶段。旧版本任务需从 ingest 重新处理以建立产物记录。
- 翻译阶段恢复重新加载已生成并核验的合并术语表；未生成术语表的任务不会读取目录中的旧文件。
- 配音恢复严格验证内部烧录视频，不再回退使用输出目录中身份不明的视频；再次导出和补回缺失字幕时刷新工作 ASS 的摘要。
- 测试使用独立的最近任务指针，不覆盖用户的恢复入口。

## 1.3.14 — 2026-09-06

- 源视频身份和任务缓存键加入完整 SHA-256；相同文件大小、相同时间戳的替换也会使旧缓存失效。
- 固定导入时的内容身份，后续处理使用经过核验的工作副本；不再将处理途中被替换的原文件信息写入旧字幕的报告。
- 恢复入口及实际导入之间再次核对身份，工作副本在烧录前及完成前校验；缓存重烧不使用被替换的工作源视频。
- 视频身份校验支持暂停 / 停止；损坏的身份记录不能作为有效缓存，零时间戳文件可正常匹配。
- 下载在替换旧缓存之前完成内容摘要和清单准备，校验途中取消保留旧缓存；默认术语文件不能被选作字幕输出。
- 更新处理修订，升级后的旧任务需从 ingest 重新处理以建立完整内容身份。

## 1.3.13 — 2026-09-06

- 处理任务及另存成品增加跨进程文件占用检查，拒绝冲突输出、覆盖活动输入和侵入其他活动工作目录；不同工作目录可并行读取同一源视频。
- 使用操作系统锁判断登记是否仍有效，异常退出后自动回收失效登记；等待登记锁时可取消。Docker Compose 共享登记目录。
- 运行开始时复制配置，避免界面或调用方修改配置后将任务输出重定向到未登记路径。
- 报告保存成片完整 SHA-256；复用外部旧成片前校验内容，已被覆盖或缺少校验信息时使用现有字幕重新烧录。
- 另存成品使用原子复制；所需成品文件缺失时，在复制任何文件前报错。

## 1.3.12 — 2026-09-06

- 成片导出、配音导出和源视频复制使用分块暂存与原子替换；复制失败、取消或检测到源文件变化时保留旧目标文件。
- SRT / ASS 先完整写入临时文件；提交第二份文件失败时回滚已提交的字幕，回滚失败则保留原文件备份并报告位置。
- 恢复任务时重新生成缺失的 SRT、导出 ASS 或工作 ASS，避免任务显示完成却没有字幕文件。
- 清理复制过程中继承了只读属性的临时文件，避免 Windows 取消任务时清理报错掩盖停止状态。

## 1.3.11 — 2026-09-06

- 修复单行字幕使用源语言识别词条拆分译文，导致译文被覆盖的问题；只有文字完整对应时才使用识别词条的时间。
- 长字幕持续拆分，保留全部文字和原始起止时间；不再为满足最短时长覆盖下一条字幕，或为满足最长时长截掉末尾时间。
- 单行字幕与配音分别使用显示片段和完整译文；恢复配音同样读取完整句子，避免重复整句或被字幕拆分打断。
- 任务报告新增 `subtitle_fit_warnings`，记录实际未满足的行长、阅读速度及显示时长限制；缓存导出也保留检查结果。
- 更新处理缓存修订，升级后重新处理旧任务，避免复用错误字幕及配音。

## 1.3.10 — 2026-09-06

- 恢复任务核对实际完成阶段；重新执行较早阶段时先撤销后续完成记录，停止时保留最后完成阶段。
- 每次处理尝试使用新任务编号，缓存报告必须与当前任务一致，避免失败的新任务复用旧字幕或成片。
- 任务状态、输入记录、报告和字幕缓存改为逐文件原子写入；无效字幕结构、非有限时间、损坏报告不再被当成可复用结果。
- 指定重跑静音检测时重新执行检测；FFmpeg 检测失败正常报错，不再写入空静音结果。
- 升级使旧处理缓存失效；恢复缺少完成记录的旧任务时，需要从 ingest 重新处理。

## 1.3.9 — 2026-09-05

- 升级后使旧处理缓存失效，防止继续复用早期缺字或时间格式错误的成片。

- 修复 WhisperX 部分句子缺少词级对齐时整句字幕丢失；完整对齐保留词级时间，不完整对齐使用整句文本和时间。
- Whisper / WhisperX 输出统一校验结构、文本及有限时间戳；无效的词级对齐不再伪造时间，保留完整识别文本。
- 每次外部识别使用独立输出目录，只提交本次成功且有效的结果；退出码为零但未写结果、坏 JSON、失败或取消均保留原结果。
- 识别输出采用原子替换，外部识别进程和后代统一清理；两个识别工作脚本及其共享模块加入打包启动检查。
- 字幕时间按总毫秒 / 厘秒舍入后分解，修复分钟、小时边界出现 60 秒的非法时间格式。

## 1.3.8 — 2026-09-05

- GPT-SoVITS 服务纳入进程树管理，停止客户端时清理服务后代；服务父进程异常退出后也不会跳过后代清理。
- 依赖探测使用受管理的子进程和临时输出文件，超时、取消或异常退出时清理后代，避免继承的管道句柄拖住探测。
- 等待其他启动任务释放锁时支持取消；启动失败后释放已退出服务的资源，允许下一次重试。
- 配音启动统一使用隔离的推理环境变量，故障日志只读取有限尾部。

## 1.3.7 — 2026-09-05

- 托管环境复用前执行依赖导入检查；缺包或检查失败时重新准备解释器并强制修复依赖，失败不保留就绪标记。
- 配音资源使用固定版本的远程内容摘要验证，损坏下载缓存自动重取；缺失语言文件或本地资源变动会触发修复。
- 资源压缩包先在临时目录完整解压，再逐文件验证并替换；解压或单文件复制失败保留对应旧文件。
- 安装子进程接入进程树清理，导入检查设置超时；关闭自动安装时不再隐式复制源码或下载资源。
- 新增 M1 MacBook Air 用户实机验收清单；GPU 实际识别和配音留待用户测试，不将构建通过视为实机验收。

## 1.3.6 — 2026-09-05

- 下载运行于独立工作进程，停止任务可终止阻塞网络请求及其子进程；Windows 使用 Job Object，Mac/Linux 使用独立进程组，异常退出也清理后代进程。
- Windows 暂停和恢复覆盖进程树，避免只暂停 Python 启动器而实际下载继续运行。
- 桌面单独下载接入暂停、恢复和停止按钮；下载与处理任务互斥，线程退出后释放按钮，主动取消不再弹错误框。
- 发布启动检查加入打包版下载入口，以及实际阻塞 HTTP 请求的取消与旧文件保护验证。

## 1.3.5 — 2026-09-05

- 下载在独立临时目录准备，成功后替换目标视频，失败或取消保留原文件；同一下载目录拒绝并发写入。
- 修复取消后继续列格式、下载及转封装的问题，进度回调响应暂停，失败尝试的临时文件不会误当成成功结果。
- MP4 转封装支持取消和原子替换；失败明确报错，不再返回随后被删除的临时路径。
- 下载记录校验视频修改信息，工作副本被替换后重新下载；只有链接标记的旧缓存不再作为下载身份凭据。
- 限制下载目录名长度并清理路径字符及 Windows 保留名称；站点识别校验实际域名，HTTPS 恢复证书验证。

## 1.3.4 — 2026-09-05

- 修复单引号、中文、括号等路径中的字幕和字体烧录；编码失败或取消时保留原有成片。
- 翻译鉴权及永久服务错误立即上报，临时网络错误最多请求四次，不再按每句重复失败请求。
- 网络重试等待支持取消；请求配置 60 秒网络超时，正在执行的同步请求仍需等待返回或超时。
- 翻译缓存纳入字符限制和词汇表；修复数字开头的译文被误删，以及无效润色结果被当作文本的问题。

## 1.3.1–1.3.3 — 2026-09-05

- Apple Silicon 原生客户端自动准备 MPS 识别与 GPT-SoVITS 依赖，GPU 不可用时回退 CPU；实机 GPU 验收仍待完成。
- 增强原视频保护、任务身份校验、配置缓存失效、工作目录互斥与子进程取消清理。
- 详细修复及验收范围见 [代码审查记录](docs/code-audit-status.md)。

## 1.2.6 — 2026-09-05

- 试听改为 PCM wav + 系统播放，打包版不再因 Qt 假成功而静音
- 目标语种为 English 时，转写本有中文就必须配音；源语种误设为 English 不再留下中文原声
- 烧录先写工作目录，只有配音成功才覆盖导出文件；未配音的成品禁止复用

## 1.2.5 — 2026-09-05

- 配音音色旁可试听；样句跟目标语种，与字幕样式无关
- 目标语种为 English 时按目标语种配音，不再因 ASR 误判成英文而留下中文原声
- 配音失败会报错，不再静默交出原声成片
- 导出文件名跟随字幕样式：英中为 `-英中字幕`，中英为 `-中英字幕`

## 1.2.4 — 2026-09-05

- YouTube 站点罐只要有 `SID` / `__Secure-3PSID` / `LOGIN_INFO` 即视为已登录，不再因缺少旧版 SID 族而跳过
- 更新仓库内 `Cookies/youtube-cookies.txt` 与 `Cookies/bilibili-cookies.txt`（仅 YouTube/Google 与 B 站域名）

## 1.2.3 — 2026-09-05

- 打包版会沿 `dist/SubFlow` 向上查找项目 `Cookies\`，B 站登录 Cookie 不再只认 exe 同级目录
- 拒绝只有访客字段的 `youtube-cookies.txt`；YouTube 启用 node/deno JS runtime
- Chrome 未运行时可用调试端口读取登录 Cookie；Chrome 127+ v20 加密库不再假装能直接读
- 下载失败时回传真实原因（机器人检测 / 412），不再一律报「无法下载最高清」

## 1.2.2 — 2026-09-05

- 中英 / 英中字幕每种语言最多 1 行；超长句缩放进安全框，不再折成多行
- 目标语种为简体时，中文轨强制转简体（修复 Whisper 默认繁体）
- YouTube / Bilibili 下载优先原声音轨，避免英文自动配音
- 源语种、目标语种、字幕样式职责拆开，互不覆盖
- 重写 README 与桌面 / 安装 / 架构 / 故障排除文档
- OpenCC 纳入主依赖，随 Win / Mac 客户端打包

## 1.2.1 — 2026-09-04

- Docker 官方镜像：`crazymsn/subflow:latest`（`docker-compose.yml` 默认拉取 Hub）
- 文档重写：字幕烧录颜色、浏览器 Cookie 兜底、BAAI 过滤、Win / Mac 发布包
- 自定义字幕颜色（桌面色块 + CLI `--zh-color` / `--en-color`）已随 1.2.0 落地，本版一并写进文档

## 1.2.0 — 2026-09-04

- 视频下载：游客失败后读取本机已安装浏览器登录 Cookie（Firefox / Edge / Safari / Chrome）
- 获取模型时屏蔽 BAAI / 智源相关条目
- 自定义字幕中英颜色；只改颜色时重渲 ASS 并重烧，不重跑识别
- GitHub Actions 按源码同时构建 Windows zip 与 macOS `.app` zip

## 1.1.0 — 2026-09-04

- 对外文案定为：SubFlow 语幕，新一代 AI 视频语音识别、自动翻译、字幕生成工具
- 重写 README 与 docs（桌面客户端、安装、令牌、故障排除、架构）
- 桌面客户端：浅色/深色纸面主题、统一微软雅黑 UI 字阶、开始栏输出路径
- 更多选项在甲板内展开，不再挡住开始栏
- 同视频只改输出路径时拷贝成品，不重跑流水线
- 勾选与按钮悬停使用同一套灯丝橙
- 发布 Windows onedir 客户端到 GitHub Releases

## 1.0.0 — 2026-09-04

- 产品名：语幕 SubFlow（深度云创科技）
- CLI：`subflow`（兼容 `bilingual-sub`）
- 保存 API 令牌后自动拉取模型列表
- Docker Compose 一键引擎
- Whisper / WhisperX、静音切句、meding 翻译、无底板烧录
- 七种界面语言；可选润色、术语、配音
- Win / Mac 客户端构建脚本与 Actions 工作流
