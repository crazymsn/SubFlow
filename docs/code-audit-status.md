# 全量代码审查进度

目标保持为审核完整项目、修复发现的问题并验证实际行为。本记录不将已通过的测试等同于“全部代码没有问题”。

## 1.3.30：固定安装器查找与构建输入

- 隔离环境中安装 uv 0.11.8，并把 uv 0.5.31 放到 PATH 前面：旧代码实际选择了 0.5.31，修复后实际选择了当前 Python 环境的 0.11.8。证据为 `.verify/uv-selection-before.json` 与 `.verify/uv-selection-after.json`。仅运行版本检查，未使用旧版安装器修改推理环境。
- 源码运行优先使用 uv Python 包提供的二进制位置，其次检查 PATH；所有候选均执行版本核验。客户端打包复用同一逻辑，防止打包时混入其他版本。冻结客户端只检查资源目录和可执行文件旁的内置位置，不回退到 PATH，也不因缺少资源目录变量而搜索当前目录。
- 固定版本必须与项目依赖一致；错误版本、非法输出或非零退出不能用于安装。版本检查有五秒超时，支持任务取消，并使用既有受控进程机制回收子孙进程。
- 旧代码五项查找回归用例失败。修复后 42 项定向用例通过，包含成功、非法输出、非零退出、超时和取消五种真实进程清理场景。定向运行触发了仅运行部分测试时的全局覆盖率门槛；全量回归另行核验。

最终全量回归 **907 项通过、1 项跳过**，核心覆盖率 **91.38%**；Ruff 与 mypy（82 个源码文件）通过。全量证据为 `.verify/uv-installer-full.log`，耗时 158.42 秒，仍有两条既有 Pillow 弃用提示。现有冻结客户端 smoke 会调用同一查找入口并执行内置 uv，打包结果交由本次提交对应的跨平台构建验证。

最终 M1 MacBook Air GPU 与试听由用户自行执行，步骤见 [Mac 自行验收](mac-self-test.md)。自动测试及依赖解析不替代该实机结果。

## 1.3.29：环境修复保持 PyTorch 构建

- 实际 uv 0.11.8 只读预演确认：按旧方式通过通用源全量重装，会将现有 torch / torchaudio `2.5.1+cu124` 换为 `2.5.1`。仅约束基础版本不能保持加速后端。旧代码六个回归用例失败，覆盖两阶段安装约束、错误构建探测和旧环境迁移。
- Windows / Linux x86_64 的两阶段安装都使用明确的 `--torch-backend cpu` 或 `cu124`，并固定完整本地构建版本。Mac 使用原生包，Apple Silicon 为 2.5.1、Intel 为 2.2.2。该选源方式依据 [uv 官方 PyTorch 集成说明](https://docs.astral.sh/uv/guides/integration/pytorch/)，并通过项目固定版本 uv 0.11.8 实际验证。
- 环境探测除导入模块外，还核对 torch / torchaudio 版本、CUDA 构建版本，以及 MPS 是否编译进 PyTorch。探测不要求当前机器有可用 GPU；实际设备可用性仍由推理时判断。健康旧环境完成探测后只更新准备标记，错误构建才进入修复。
- 每个环境在自身安装锁下保存约束文件，消除不同运行环境同时写同一约束文件的情况；清除继承的 UV_TORCH_BACKEND，采用应用明确选择的后端。

34 项定向测试、Ruff 与 mypy（81 个源码文件）通过。ASR 与 GPT-SoVITS 的 Linux CPU、Mac arm64、Mac Intel 共六组依赖解析通过，选择了预期的 PyTorch 版本；Linux CPU 解析未引入 nvidia-* CUDA 运行库包。现有 CUDA 环境通过实际 GPT-SoVITS 导入和构建探测，按 CPU 构建要求检查同一环境时被正确拒绝；验证没有修改已有环境。Mac 解析不等于实机安装或 GPU 推理验收。

证据为 `.verify/torch-repair-old-resolver.log`、`.verify/torch-repair-cu124-resolver.log`、`.verify/torch-build-live-acceptance.json`，以及 `.verify/torch-*-resolved.txt` / `.verify/sovits-*-resolved.txt`。M1 MacBook Air 实机 GPU 与实际试听仍由用户最后自行验收。

首轮全量回归发现生命周期测试的 PID 通知竞态：测试收到文件创建后立即关闭子进程，PID 可能尚未写入。测试夹具现完整写入临时文件后原子发布，保留对子孙进程实际退出的检查；9 项生命周期测试通过。该失败证据为 `.verify/torch-build-full.log`。

修复后的最终全量回归 **893 项通过、1 项跳过**，核心覆盖率 **91.33%**；Ruff 与 mypy（81 个源码文件）通过。全量证据为 `.verify/torch-build-full-final.log`。测试环境为隔离的 FastAPI 0.141.1 / Starlette 1.6.0，仍有两条既有 Pillow 弃用提示。

[1.3.29 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33990195478) 的 Windows、Mac arm64、Mac Intel 和 Docker 四项构建全部通过。

## 1.3.28 补充验收：真实进程异常退出

新增独立子进程测试，在识别文件写完但阶段记录未提交、外部 ASS 已替换但工作 ASS 尚未替换、完成报告已替换但 done 状态尚未替换这三个位置执行 `os._exit(77)`，确保跳过 finally 清理。三个场景均通过：

- 未完成的识别与字幕阶段不能被后续阶段跳过；中断前完成阶段保持准确。
- 工作目录锁可重新获取，崩溃遗留的资源登记由后续任务依据实际锁状态回收。
- 从正确阶段恢复后，报告与状态的任务标识相同、外部字幕摘要匹配记录；识别阶段重新识别，字幕及完成记录阶段不重复识别。

本轮尚未发现这些边界上的运行代码缺陷，新增测试已纳入常规 pytest 收集。识别、音频提取和视频探测使用固定夹具，文件提交、进程退出、操作系统锁及恢复流程为实际执行。它验证进程退出后的恢复，不代表断电时的磁盘持久性，也不宣称多个文件在崩溃瞬间已经整体原子提交。定向证据为 `.verify/process-crash-recovery.log`。

新增用例后的全量回归 **880 项通过、1 项跳过**，核心覆盖率 **91.33%**，Ruff 通过。运行代码和版本仍为 1.3.28；此轮提交用于补充跨平台持续集成验收。全量证据为 `.verify/process-crash-full.log`。

[补充验收 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33989381706) 的 Windows、Mac arm64、Mac Intel 和 Docker 四项构建已全部通过；三个客户端的 pytest 包含新增的真实进程崩溃恢复场景。

## 1.3.28：完成记录保存失败与取消

- 四个旧代码用例复现了两个问题：报告写入后状态保存失败会留下新报告与旧状态；最后一份输出摘要计算结束时收到取消，任务仍会写入完成报告。覆盖新处理与自动重新导出。
- 拆分状态记录生成与写入，最终报告和 done 状态共用 JSON 集合提交。先完整序列化和暂存，再提交；任一替换失败时恢复原记录，首次失败时移除已经提交的新报告。
- 准备记录和备份期间仍检查暂停 / 停止；开始替换后完成整组提交或回滚。提交开始后到达的停止不会再把已完成状态标为 stopped，避免“完成”和“已停止”同时出现。自动重新导出被取消时也记录 stopped，并保留此前完成阶段。
- 最近任务指针在自动重新导出的完成记录提交前保存，避免指针保存错误发生在 done 记录已经提交之后。元数据集合回滚针对报告的 I/O 错误，不保证强制终止或断电时多个文件整体原子；字幕、视频和元数据仍是不同提交集合。

115 项定向测试通过，包含保存失败后不重复识别的恢复、首次失败不遗留成功报告，以及提交开始后的取消状态一致性。真实 FFmpeg 创建含音轨视频，完成提取和烧录后注入状态替换失败：原报告及状态保留，恢复没有重复识别；最终摘要后取消保留原报告并记录停止，原成片摘要不变、FFprobe 正常，临时文件清理完成。识别使用固定文本夹具，实际音频提取、烧录和探测均执行。证据为 `.verify/completion-records-live/acceptance.json`；旧代码失败记录为 `.verify/completion-records-baseline.log`。

最终全量回归 **877 项通过、1 项跳过**，核心覆盖率 **91.33%**；Ruff 与 mypy（81 个源码文件）通过。新增测试统一按 UTF-8 读取 JSON 后，68 项恢复测试再次通过，另在独立中文目录执行的 8 项完成记录测试全部通过。全量测试使用隔离的 FastAPI 0.141.1 / Starlette 1.6.0 环境，仍有两条既有 Pillow 弃用提示。

后续继续检查缓存记录在异常中断后的识别与恢复。M1 MacBook Air 实机 GPU 与实际试听仍由用户最后自行验收。

## 1.3.27：恢复字幕与三份字幕的提交

- 旧代码在 burn / dub / done 恢复时只检查外部字幕是否存在，可能将原位置或新位置已有的无关字幕记录为成功结果。六个旧代码回归用例均失败；现从已核验的字幕数据重新生成外部 SRT / ASS。
- 缺失文件的恢复原先会更新渲染记录，绕过原样式检查。现先检查原记录及渲染设置；重建 ASS 的内容必须与原摘要相同。已有但内容改变的工作 ASS 仍拒绝复用。三个缺失文件加样式变化的旧代码用例均失败，修复后通过。
- 初次渲染、自动重新导出与阶段恢复共用字幕提交过程：先在独立临时目录生成文本，再统一暂存外部 SRT、外部 ASS 和工作目录中的烧录 ASS，提交前重检样式。任一替换失败时回滚之前已提交的文件；两种工作 ASS 替换失败用例证明了旧代码遗留外部变更，修复后均恢复。
- 处理修订改为 `restored-subtitle-set-v13`，旧任务需重新处理。三份字幕的回滚针对正常返回的 I/O 错误；它不保证进程被强制终止时的整体原子性，视频与字幕也尚未纳入同一提交集合。
- 后续故障注入确认自动重新导出在报告写入前已发出 done / 100%：报告写入失败和正常写入时完成回调读取旧报告的两个旧代码用例均失败。现将完成事件移到报告、任务状态和最后任务指针保存成功之后；两个用例修复后通过。证据为 `.verify/report-persistence-probe/evidence.json` 和 `.verify/report-progress-baseline.log`。

127 项定向测试、Ruff 与 mypy（81 个源码文件）通过。实际 FFmpeg 创建并处理两秒含音轨视频，替换外部字幕后从 done 恢复，字幕摘要与原结果一致、成片未改变；注入工作 ASS 提交失败后，三份字幕和原成片均保持，临时文件清理完成。识别使用固定文本夹具，音频提取、烧录和 FFprobe 为实际执行。证据为本地 `.verify/subtitle-set-live/acceptance.json`；旧代码失败记录为 `.verify/subtitle-resume-baseline.log` 与 `.verify/subtitle-set-baseline.log`。

完成事件顺序修复后的最终全量回归 **871 项通过、1 项跳过**，核心覆盖率 **91.32%**；Ruff 与 mypy（81 个源码文件）通过。测试环境为隔离的 FastAPI 0.141.1 / Starlette 1.6.0，仍有两条既有 Pillow 弃用提示。

后续继续检查报告与任务状态在中断时的一致性。M1 MacBook Air 实机 GPU 与实际试听仍由用户最后自行验收。

[1.3.27 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33988478230) 的 Windows、Mac arm64、Mac Intel 和 Docker 四项构建已全部通过，包含音频检查与客户端启动检查；不替代 M1 实机 GPU 和实际试听验收。

## 1.3.26：导出复制绑定已核验的内容

- 复现了校验与复制之间的缺口：在完成缓存 SHA-256 检查后、复制开始前替换同大小内容并恢复时间戳，旧代码仍导出错误内容。三个旧代码回归用例均未按要求拒绝该替换，覆盖原声成片及两种配音导出方式。
- `copy_file` 支持预期摘要，在复制的同一轮分块读取中计算实际内容摘要，匹配后才提交目标；同路径 / 硬链接无需复制时也核验。没有传入摘要的内部通用复制保持原行为。
- 缓存上下文校验返回已验证记录，供后续复制使用；原声烧录、带配音成片、配音旁路文件与旧成片重新导出都绑定该记录。GUI 多文件重定位原有的暂存后摘要核验保持不变。
- 更新处理修订为 `verified-export-bytes-v12`：旧逻辑可能已经将错误导出内容记录进报告，因此旧任务需要重新处理。该改动证明复制内容与记录一致，不代表多个导出文件已经形成整体原子提交，也不防止提交后外部应用再次修改文件。

定向测试包括复制前 / 复制中保持元数据不变的内容替换、硬链接、取消和 I/O 错误保护。另用实际 FFmpeg 创建含音轨 MP4，正确复制后在下一次复制的检查点替换源文件头部并恢复元数据，确认被拒绝、原目标 SHA 不变、仍可由 FFprobe 正常读取，且临时文件清理完毕。证据为本地 `.verify/verified-export-live/acceptance.json`；旧代码失败记录在 `.verify/export-copy-baseline.log`。

最终全量回归 **858 项通过、1 项跳过**，核心覆盖率 **91.32%**；Ruff 与 mypy（81 个源码文件）通过。测试环境为隔离的 FastAPI 0.141.1 / Starlette 1.6.0，仍有两条既有 Pillow 弃用提示。

后续继续核对字幕与视频输出集合的提交及恢复。M1 MacBook Air 实机 GPU 与实际试听仍由用户最后自行验收。

[1.3.26 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33987537234) 的 Windows、Mac arm64、Mac Intel 和 Docker 四项构建已全部通过，包含音频检查和客户端启动检查；M1 实机 GPU 与实际试听仍待用户验收。

## 1.3.25：试听播放生命周期与过期结果

- 原 Mac 路径丢弃 `afplay` 子进程句柄，仅按音频时长发送完成信号，停止或退出不能管理该进程。现改为 QProcess，使用 started / finished / error 事件；停止时处理启动竞争、终止并回收自己的子进程，播放超时也会结束任务。
- 原 Qt 路径在 `play()` 返回后立即检查播放状态，将正常异步加载误当成失败，后续解码错误也没有反馈。现等待状态 / 错误 / EndOfMedia 信号，加入等待上限，并通过对象身份忽略旧播放器及旧计时器事件。播放器、输出对象和计时器均安排释放。
- 完成信号只发送一次；停止旧任务不会完成新任务。Windows 仅清除本实例发起的 winsound 播放；外部系统播放器打开失败会报错，打开成功后的播放由外部应用管理。
- 界面在收到真实播放开始事件后记录“正在试听”，播放期间保持忙碌状态。结果返回时核对端点、参考音频、提示文字、语种及所选视频；从视频提取参考时，在合成结束后再次核验原视频内容。旧线程错误与关闭后的回调不会改写当前状态。

26 项定向测试通过，包含实际 QProcess 子进程的正常退出、失败、缺失程序、启动后立即停止及回收。Mac 分支通过替代 Python 子进程在当前 Windows 环境验证，没有运行 macOS 的 afplay。另以真实 Qt Multimedia 播放 0.2 秒静音 PCM，观察到 started / finished 且对象引用释放，证据为本地 `.verify/playback-live/acceptance.json`；这不代表扬声器听感验收。

后续继续核对导出结果与错误恢复边界。M1 MacBook Air 实机 GPU 与实际试听仍由用户最后自行测试。

最终全量回归 **850 项通过、1 项跳过**，核心覆盖率 **91.29%**；Ruff 与 mypy（81 个源码文件）通过。测试环境为隔离的 FastAPI 0.141.1 / Starlette 1.6.0，仍有两条既有 Pillow 弃用提示。

[1.3.25 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33986682398) 的 Windows、Mac arm64、Mac Intel 和 Docker 四项构建已全部通过，包含音频后处理 / 编码检查与客户端启动检查。该结果不替代 M1 实机 GPU 和实际试听验收。

## 1.3.24：音频数值与编码边界

- 对修改前实际 `audio_postprocess` 的复现确认：正峰值被转换为 `-32768`；24 kHz 输出按配置中的 32 kHz 插入停顿；输入张量被原地归一化；NaN 可进入整数 PCM。修复后采用有限值检查、非原地处理与饱和转换，正峰值为 `32767`，停顿按输出采样率计算。
- 普通输出、超采样输出及超采样模型缺失分支共用转换。无有效形状、空波形和非有限波形抛出明确音频错误，不触发无关的模型重载。语音缓存修订改为 `saturated-pcm-v5`。
- OGG 线程内编码异常传播到 API 调用方，栈设置恢复；AAC 拒绝非零退出码和空输出，设置 300 秒编码上限，超时后 kill / communicate 回收子进程。编码错误使用输入 / 输出错误类型，避免误判成 MPS 故障而重载模型。
- 新增 `scripts/check-sovits-audio.py`，使用实际函数源码、真实 CPU Torch 张量和实际编解码库验证后处理与 RAW / WAV / OGG / AAC。三个客户端和 Docker 工作流在准备好推理依赖后执行，不需要下载神经网络权重。开发测试增加 NumPy 依赖。

本机真实 CPU 模型完成两次合成，生成的四种编码均能解码为非零 PCM，WAV / RAW 样本逐值一致。旧函数和新函数对比以及实际编解码证据为本地 `.verify/audio-boundaries-live/acceptance.json`。超采样分支的数值输入使用替代模型输出验证，没有加载真实超分辨率权重；边界脚本不代表 GPU 或感知音质验收。

后续继续检查有效音频的质量判定与播放 / 导出边界。M1 MacBook Air 实机 GPU 测试仍由用户最后自行执行。

在 FastAPI 0.141.1 / Starlette 1.6.0 隔离环境中完成 **833 项通过、1 项跳过**的全量回归，核心覆盖率 **91.29%**；Ruff 与 mypy（81 个源码文件）通过。版本及缓存修订更新后，28 项配音适配器测试再次通过。无权重验收脚本的 worker 在本机 Torch 2.5.1 环境以 CPU 运行通过。

[1.3.23 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33985441512) 的 Windows、Mac arm64、Mac Intel 和 Docker 四项构建已全部通过。本轮 1.3.24 新增的跨平台音频检查待构建执行。

## 1.3.23：配音缓存绑定实际模型状态

- 每次完整模型事务成功后生成新的随机版本标识，和模型状态一起提交；失败保持原标识。服务重启、同路径重载及 CPU 回退也产生新标识，不将相同路径视为相同内存模型。
- `/subflow/runtime` 返回标识；GET / POST 请求可指定 `model_revision`。非流式请求在模型锁内拒绝旧版本，音频响应头返回实际生成版本。排队期间语言支持发生变化也在执行前重新校验。
- 试听、逐句原始及拟合音频将模型标识纳入缓存键。模型变化时整次操作最多重试一次；CPU 回退后全部句子使用新版本重新生成。混音先写临时成片，再核对模型状态并提交，避免覆盖已有正确成片。
- 成品报告及 dub 阶段记录保存实际模型版本；自动复用和 done 恢复要求当前模型相符。旧服务没有版本扩展时仍可合成，但无法证明模型一致性，故禁用其跨任务音频缓存与已完成配音缓存复用；这类服务的运行中模型变化也不能靠本协议检测。离线或重启后不能将旧配音当作当前模型结果重新导出，需要启动服务并从 dub 重做。已导出的文件仍可直接使用。
- 中文源视频目标简体 / 繁体时保持原声，新增测试确认处理过程不查询配音模型。识别与字幕处理的修订未改变，本轮只新增模型状态校验。

真实 CPU 服务在独立端口启动并正常退出：同一版本复用实际试听；缺失权重加载失败后版本和缓存不变；成功重载实际 GPT 权重后版本变化，旧版本请求返回 409，再次试听生成非零 PCM。证据为本地 `.verify/model-identity-live/acceptance.json`。自动 CPU 回退后的整段重试使用可控合成器验证，尚无此轮 MPS 实机证据。

后续继续审查音频数值转换与输出质量边界。M1 MacBook Air 实机测试仍由用户最后自行执行。

最终在 FastAPI 0.141.1 / Starlette 1.6.0 隔离环境中完成 **808 项通过、1 项跳过**的全量回归，核心覆盖率 **91.29%**；Ruff 与 mypy（81 个源码文件）通过。新增测试覆盖版本协议、锁内重检、响应版本变化、缓存失效、整段重试、done 恢复、中文原声不查询模型，以及暂存输出不能覆盖源视频 / 参考音频。

[1.3.22 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33984755137) 的 Windows、Mac arm64、Mac Intel 和 Docker 四项构建已全部通过。本轮 1.3.23 的构建验证待提交后执行。

## 1.3.22：模型与配置切换事务

- 新权重在独立候选状态中加载，共享的旧模型只读；配置、参考缓存及声码器设置隔离。嵌套加载延迟到所有组件成功后再保存，配置文件使用 UTF-8 暂存、fsync 和原子替换，最后一次性发布内存状态。
- 缺失 GPT / SoVITS 权重、后期组件异常、磁盘保存失败不会改变原模型、原缓存或原文件。设备、精度和错误恢复沿用相同机制；`save=False` 只提交内存。CPU/MPS 别名规范化，失败的 CPU 回退不提前改变当前 MPS 配置。
- BERT / HuBERT 成功加载后更新配置路径，语言校验读取当前配置对象。SoVITS 版本变化时重算静音令牌派生值，计算不保留梯度历史。新版本不需要的声码器及说话人模型在提交后释放。
- 保留旧模型直到候选成功意味着切换时需要同时容纳部分新旧权重；内存不足时会报告加载错误并保留旧状态，不保证任意内存容量都可切换。处理及语音缓存修订升级，旧任务需重新处理。

真实 CPU 测试加载实际模型，注入缺失权重、SoVITS 加载后期失败、配置保存失败和设备迁移失败，逐次核对模型对象、完整缓存及配置文件均保持原样，随后实际合成非静音音频。成功重载后新模型与空缓存一起生效；CPU 别名重建和 `save=False` 验证通过。证据位于本地 `.verify/reference-cache-live/model-switch.json`。此轮没有执行真实 MPS / CUDA 迁移；失败迁移使用加载函数注入异常。

后续继续核对运行中模型变更与客户端成品 / 试听缓存身份的一致性。M1 MacBook Air 实机测试仍由用户最后自行完成。

最终在 FastAPI 0.141.1 / Starlette 1.6.0 隔离环境完成 **779 项通过、1 项跳过**的全量回归，核心覆盖率 **91.19%**；Ruff 与 mypy（80 个源码文件）通过。新增覆盖候选状态回滚、嵌套加载单次提交、文件替换 / fsync 失败、当前配置语言校验及失败 CPU 回退。

[1.3.21 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33984125603) 的 Windows、Mac arm64、Mac Intel 与 Docker 四项构建已全部通过；这是上一版本构建结果，本轮 1.3.22 尚待构建验证。

## 1.3.21：推理参数与无有效语音状态

- API 与直接调用 TTS 共用参数校验，在随机种子、参考特征和模型推理之前拒绝非法输入。覆盖批大小为零、正数参数为零或负数、概率越界、NaN / 无穷及无法表示的浮点大小、随机种子边界。保留直接调用时空种子代表随机的兼容行为。
- 预处理返回空片段、流式分段全部不可发音及合成停止时抛出明确异常；不再返回一秒静音。正常流式片段之间的静音间隔保留。此类状态不触发 MPS 回退或模型重载。
- 流式响应在发送 HTTP 成功状态前预取第一块音频；首块失败返回输入 / 合成错误。预取迭代器支持发送第一块之前关闭，避免悬挂模型锁；断连清理测试继续覆盖两个 ASGI 协议版本。
- 修复并行采样 `top_k=0` 访问空 top-k 结果的问题，实际 Torch 张量验证其概率与完整 softmax 一致。任务和语音缓存修订更新，旧缓存需要重新处理。

真实 CPU 模型验证包含正常合成后注入空预处理结果、停止标记与无有效流式片段，确认没有音频产出且 GPT 模型实例未被重载；空白文本与零批大小在推理前拒绝。证据为本地 `.verify/reference-cache-live/validation.json`，其中空片段和停止用可控注入触发，非真实翻译输入。此轮未做 MPS 实机推理。

下一轮继续审查模型切换失败后的配置和模型完整状态。M1 MacBook Air 实机验收由用户最后自行执行。

最终在 FastAPI 0.141.1 / Starlette 1.6.0 环境完成 **771 项通过、1 项跳过**的全量回归，核心覆盖率 **91.19%**；Ruff 与 mypy（80 个源码文件）通过。HTTP GET / POST 的非法参数、库调用类型与边界、四种流式模式的空语音错误及预取后关闭均有回归覆盖。

## 1.3.20：参考特征缓存与新依赖兼容

- 主参考使用完整内容摘要，同路径音频替换后必须重新提取。辅助参考使用有序内容身份，内容改变、重新排序和删除均刷新；缺失文件报错，不悄悄忽略用户指定音色。
- 主参考的语义、频谱与原始波形在临时缓存中准备，最终再次核验文件。辅助参考失败也回滚本次主参考更新；辅助特征提取不再把主参考原始波形替换成最后一个辅助参考。提示特征一次提交，缓存键包含文本、语言及模型版本。
- 重载 GPT / SoVITS / BERT / HuBERT 权重，以及切换设备或精度时，旧特征失效。重载 BERT 重建文本预处理器；设备切换同步其设备字段。CPU 与 MPS 禁用半精度，切换到这些设备时转回单精度。
- 1.3.19 客户端 CI 使用 FastAPI 0.141.1，其 GET 未指定类型的列表被判为表单字段；三个客户端均在接口契约测试失败，Docker 成功。明确声明 `list[str]` 查询参数后，在隔离的 FastAPI 0.141.1 / Starlette 1.6.0 环境中，10 项接口契约测试通过。
- 处理修订与配音请求指纹均升级。旧任务需重新处理，旧试听及逐句音频缓存不能被当作修复后的正确音色继续复用。

真实 CPU 对比先保存并加载修改前 TTS 源码：两次合成之间替换同一路径参考音频，旧版本仅提取 1 次参考特征；修复版提取 2 次，两次生成均为非静音音频。随后重载 GPT 权重，确认缓存清空，第三次合成重新提取；重载 BERT 确认预处理器引用新模型，CPU 半精度设置被拒绝。证据位于本地 `.verify/reference-cache-live/baseline.json`、`fixed.json`、`model-reload.json`。此轮未做 MPS 实机推理，辅助参考及失败回滚通过可控特征提取器测试。

后续仍需审查模型切换失败后的完整状态、非法合成参数及无有效文本时的行为。M1 MacBook Air 实机测试由用户最后自行执行。

最终在 FastAPI 0.141.1 / Starlette 1.6.0 隔离环境运行全量回归，**717 项通过、1 项跳过**，核心覆盖率 **91.19%**；Ruff 与 mypy（80 个源码文件）通过。该环境复用已有 Python 依赖，仅覆盖这两个接口框架版本，没有修改用户现有环境。

[1.3.20 GitHub Actions](https://github.com/crazymsn/SubFlow/actions/runs/33983518357) 的 Windows、Mac arm64、Mac Intel 与 Docker 四项构建已全部通过。

## 1.3.19：服务端并发、断连与 CPU 请求等待

- 非流式推理移出 HTTP 事件循环，服务端用同一把异步锁保护合成、参考音频设置和权重修改。健康接口增加 `busy` 状态，合成期间仍可响应。
- 流式推理持锁至生成器结束或关闭。客户端取消无法终止正在执行的 Python 推理线程，因此保持模型占用直到该线程退出，随后才允许下一个模型操作。
- 实际 HTTP 断连模拟复现了框架在发送阶段取消时没有立即关闭生成器的问题；响应退出现在显式关闭异步迭代器。回归覆盖 ASGI 2.0 断连消息与 2.4 发送异常，并要求清理和释放锁在响应结束时已经发生。
- GET 缺少语言参数返回输入错误，POST 使用 Pydantic 2 接口；客户端配音读取超时由 180 秒延长到默认 1800 秒，并允许通过 `SUBFLOW_GPTSOVITS_TIMEOUT` 调整。非法值明确报错，停止请求仍可打断客户端等待。
- 直接调用配音适配器也禁止覆盖参考音频，使用独立暂存文件，避免固定 `.part` 名称与已有文件冲突。

真实验证启动了独立 CPU GPT-SoVITS 服务，合成得到 **5.64 秒非静音音频**；推理期间 **25 次**健康检查返回成功，最长约 **0.45 秒**。接口报告 `device=cpu`、`is_half=false`，测试服务正常退出。证据为本地 `.verify/server-concurrency-live/acceptance.json`；该测试没有验证 Apple MPS。并发、取消及 MPS 预处理回退的接口契约测试使用可控模型替身，不计作真实 GPU 推理证据。

后续继续审查上游参考缓存：当前 `TTS.run` 比较参考路径与提示文本，仍需核对同路径内容替换、提示语言改变、辅助参考和模型切换后的缓存刷新。M1 MacBook Air 由用户最后自行验收。

最终本地回归 **704 项通过、1 项跳过**，核心覆盖率 **91.19%**；Ruff 与 mypy（80 个源码文件）通过。接口契约测试直接加载仓库中的 API 入口，替换重型模型依赖，实际执行健康检查、合成、模型修改和 HTTP 断连行为。

## 1.3.18：成品另存事务与下载占用

- GUI 另存核对报告与完成状态的任务编号，完整校验源视频和各成品的 SHA-256。相同大小、时间戳的内容替换以及原路径重复导出均不能绕过校验。旧报告缺少成品校验记录时提示重新导出或处理。
- 视频、字幕、配音成片和报告先全部准备，再依次提交；任一提交失败恢复先前内容。包含只读暂存文件清理、失败回滚时保留备份并明确报错。此机制处理可捕获的异常，不保证进程被强制结束时的跨文件原子性。
- 报告与任务状态参与全局占用检查，界面仅在成功后更新路径。独立下载也预留视频、日志及工作目录；已持有整个工作目录的流水线下载复用父任务占用。
- 下载错误日志原子保存，日志保存失败保留原始异常。1.3.17 的 Apple arm64 CI 在暂停后停止下载测试中返回 `PermissionError`，其余三个构建成功；新增退出进程组检查，权限错误仅在组无存活成员时容忍，仍有存活成员则报错。[1.3.18 构建](https://github.com/crazymsn/SubFlow/actions/runs/33982392091)的 Windows、Mac arm64、Mac Intel 与 Docker 四项已全部通过。

本地完整回归 **687 项通过、1 项跳过**，核心覆盖率 **91.19%**；Ruff 与 mypy（80 个源码文件）通过。真实媒体验证恢复已有中文转繁体任务，得到 2 条字幕、4.08 秒原声成片；各成品摘要匹配，另存成功，注入报告提交失败后恢复全部旧文件。证据保存在本地 `.verify/relocate-live/acceptance.json`，此次未重新执行识别、翻译、语音合成或 MPS 推理。

M1 MacBook Air 实机验收由用户最后自行完成，见 [自测步骤](mac-self-test.md)。全量审查仍需继续核对第三方推理运行边界；当前测试通过不等于所有平台或所有视频均已完成验收。

## 1.3.17：试听参考身份与已完成媒体恢复

- 试听请求的完整身份及音频摘要参与缓存判断，覆盖语言、音色、端点、参考文件内容、提示文本和实际试听台词。显式复用同一个输出路径也必须匹配请求；文件名摘要基于未清理的原始参数，避免不同汉字或特殊字符清理后的碰撞。兼容的旧云端引擎名称统一映射到 GPT-SoVITS 的准备流程。
- 试听输出和记录不能覆盖参考文件；音频验证后原子替换，失败保留旧音频及记录。试听登记参考输入与输出占用，拒绝冲突任务。自动截取参考音频绑定完整源内容与区间，GUI 后台任务也使用完整内容身份；短音频、取消、源内容在提取期间变化均不能提交为有效新参考。
- 渲染、烧录和配音完成记录绑定实际字幕样式、编码设置、工作 ASS 摘要及对应配音身份。恢复时比较当前请求，要求从设置变化所影响的阶段重新处理。处理中参考内容或样式变化不能在完成时被重新标记为此次产物的身份。
- 非配音任务也保留内部 `burned.mp4`。恢复时从已核验的内部媒体复制到实际输出位置，不信任被替换的外部成片。`done` 恢复配音任务会导出 `dubbed.mp4` 并填写正确输出路径；缺失或损坏时明确报错。
- 烧录 / 配音完成与公共输出复制分开记录。复制失败保留有效内部媒体；配音导出失败说明可从 done 重试。恢复保留翻译调用统计及缺译列表，不再清零。状态和对应上下文一次提交；仍不声称整个媒体任务是跨文件原子事务。
- 处理修订更新，旧版本任务需重新处理建立新的上下文。内部烧录媒体是恢复依据，需要保留；删除后应从 burn 重新处理。

回归覆盖显式试听路径切换请求、有效音频同大小 / 时间戳替换、参考碰撞与占用、取消和截断、源参考区间改变、GUI 自动参考路径、配音 / 原声任务从 done 恢复到新位置、成片损坏、字幕和音色设置变化、复制失败后免合成恢复、翻译统计保持。

真实验收分别使用现有 CUDA GPT-SoVITS 服务、CPU Whisper tiny 与 FFmpeg：试听相同请求复用，切换语言重新合成；中文转繁体产生 2 条字幕且保留原声，从 done 导出的新成片与原成片 SHA-256 相同，没有重复识别或烧录。另用固定转写 / 翻译输入和真实 GPT-SoVITS 合成，模拟成片导出被占用：已有输出保留，恢复时不增加语音合成次数，成功导出 4.08 秒成片。两组证据分别为本地 `.verify/preview-resume-live/acceptance.json`、`.verify/dub-done-live-final/acceptance.json`；固定转写 / 翻译输入的测试不计作真实翻译服务验收，此次也没有重验安装或 MPS。

最终本地完整回归 **664 项通过、1 项跳过**，核心覆盖率 **91.12%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **80 个源码文件**通过。新版客户端与 Compose 由本次提交对应的 GitHub Actions 构建验证。

全量审查继续，后续需核对 GUI 成品移动与独立下载入口的占用 / 恢复，以及第三方推理运行边界。M1 MacBook Air 实机测试由用户最后自行执行。

## 1.3.16：逐句配音缓存与混音提交

- 原始配音及拟合 WAV 使用包含完整请求身份与 SHA-256 的旁置记录。旧的无记录缓存不复用；JSON 序列化请求避免分隔符歧义，拟合身份包含原始音频实际摘要与目标时长。
- 按块验证 PCM WAV 声明的帧数，空文件、截断数据和非 PCM 数据不能提交为有效缓存；目标时长必须为有限正数。原始音频、拟合音频均先在独立暂存路径产生，验证后替换。
- 原始音频重新合成后，拟合缓存随实际内容改变。拟合后复查源音频，混音前复查各句拟合缓存；参考音频及配音设置在合成过程中发生变化时拒绝提交旧身份的记录。
- 最终混音写入与目标同目录、保留媒体扩展名的独立暂存文件；完成且没有取消后再替换旧视频。失败、空输出、取消和编码返回后停止均保留旧成片；输出不能覆盖输入视频或音频。
- 音频与旁置记录是两个文件，不声称跨文件原子事务。媒体替换后记录提交失败时，摘要不匹配会阻止后续复用；已经完成的其他句子仍可复用。处理修订更新以拒绝旧整任务缓存。

定向测试使用真实 PCM WAV 与 FFmpeg 拟合，覆盖有效缓存复用、同长度 / 时间戳内容替换、坏记录、截断合成、拟合途中源音频变化、参考变化、混音前已处理句子改变、提交记录失败、取消、非法时长及输入覆盖。最终混音的故障注入覆盖旧成片保护；已有多句长路径测试继续限制 Windows 命令长度。

真实 GPT-SoVITS 服务与 FFmpeg 验收：连续三次配音仅发起一次合成，第三次修改拟合缓存后重新拟合；成片时长 6 秒，解码音频非静音。服务配置为 CUDA / FP16，此次没有重启服务或重新准备依赖，不能计为新安装、CPU 或 MPS 推理验收。最终代码的证据在本地 `.verify/audio-live-final/acceptance.json`。

最终本地完整回归 **624 项通过、1 项跳过**，核心覆盖率 **90.77%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **80 个源码文件**通过。[GitHub Actions 33980767201](https://github.com/crazymsn/SubFlow/actions/runs/33980767201) 整体成功，Windows、Apple Silicon、Intel Mac 客户端与 Docker / Compose 检查通过。

后续仍需审查试听缓存、恢复至完成阶段的成片处理以及强制退出的跨文件恢复；全量审查保持进行。M1 MacBook Air 最终实机验收由用户自行执行。

## 1.3.15：阶段缓存内容验证与术语表恢复

- 音频提取、静音区间、转写、源字幕、生成 / 合并术语表、翻译字幕、排版字幕、工作 ASS、内部烧录视频及配音视频的内容摘要，与阶段完成信息写入同一原子状态记录。文件名使用固定允许列表；缺少字段、非法摘要和不完整的术语表记录不能通过。
- 恢复在读取所需缓存之前核验；文件缺失、合法 JSON 内容被替换或媒体改变时指出应重跑的生产阶段。自动导出检查所需源字幕、译文、排版字幕和配音成片，失败则重新处理。未被该路径消费的临时文件不影响正常导出。
- 重跑较早阶段时同时丢弃后续阶段记录；计算摘要支持暂停 / 停止，取消不会将未完成阶段标记为成功。重新导出 / 补回缺失字幕后更新工作 ASS 摘要，允许继续恢复。
- 从 translate 恢复时加载经过核验的合并术语表，修复此前生成术语丢失的问题。未生成术语的阶段明确记录空产物，忽略遗留文件。
- 配音开始前验证内部烧录视频；丢失或被修改时不启动合成服务，也不使用输出路径上的其他成片顶替。
- 测试隔离最近任务指针，避免测试任务覆盖用户的恢复入口。升级处理修订后，旧任务需重新处理以建立内容记录。

回归涵盖各阶段产物改变、合法 JSON 替换、缺失排版字幕、导出校验后源字幕变化、术语恢复与损坏、内部烧录丢失 / 被替换、配音成片变化、重复导出及摘要取消。这里的摘要用于检测意外修改和缓存错用，不是针对同时篡改产物与状态记录的认证机制。

真实 Windows CPU Whisper tiny 与 FFmpeg 验收识别得到 2 条字幕：替换合法译文 JSON 后，恢复被拒绝且已有输出保持不变；从 translate 重跑不重复识别，字幕恢复一致，后续导出成功复用。中文目标繁体没有配音；输出与源片解码音频相关系数约 0.999979、信噪比约 43.62 dB。烧录的 AAC 重编码用于时间戳归一化，音频并非逐字节相同。证据保存在本地 `.verify/cache-live/acceptance.json`；此次使用现有 Python 环境，没有重新验收依赖安装或 Apple GPU。

最终本地完整回归 **600 项通过、1 项跳过**，核心覆盖率 **90.34%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **79 个源码文件**通过。[GitHub Actions 33980173973](https://github.com/crazymsn/SubFlow/actions/runs/33980173973) 整体成功，Windows、Apple Silicon、Intel Mac 客户端与 Docker / Compose 检查通过。

尚未覆盖 TTS 每句原始 / 时长拟合 WAV 的独立内容记录，跨文件强制退出恢复也需继续审查。Apple M1 实机测试由用户最后自行运行；CI 的 arm64 打包成功不等于实际 MPS 推理验收。

## 1.3.14：源视频内容身份与工作副本

- 本地及下载源视频身份加入完整 SHA-256，缓存键包含内容摘要。读取过程中检查大小、修改时间及文件标识稳定，并在分块读取时响应任务暂停 / 停止。损坏身份记录直接判为不可复用，零时间戳不再被误当成缺失值。
- 在复制前捕获输入身份，复制后验证工作副本内容相同。两种路径设置都使用工作副本参与处理；原先关闭 ASCII 路径选项时已经会复制文件，现在后续步骤也使用该副本。
- 报告保留导入时的输入身份和处理配置，不在任务结束时将外部已修改文件重新认作这次输入。输入文件在识别后被替换时，成片和字幕仍对应原工作副本，下一次任务不会复用为替换后的输入。
- 恢复时在目录校验后、真正复制前再次核对输入和处理配置，拒绝校验间隙发生变化的文件。导入完成记录移至身份记录成功写入之后；URL 标记本身不足以证明可恢复。
- 工作副本在开始使用、烧录前和最终报告前核验。缓存重烧优先验证工作源视频，损坏时可使用内容匹配的原文件；两者都不匹配则要求重新处理。处理期间术语文件的持久变化会阻止写出成功报告。
- 下载在旧缓存提交前准备完整摘要及清单，哈希取消时原视频与旧 URL / 清单保持不变，并覆盖只读临时视频清理。默认术语文件也纳入输入保护与占用登记，不能被字幕输出覆盖。
- 更新处理修订以拒绝缺少可靠输入身份的旧处理缓存。该检查尚不等于所有中间音频、转写、字幕、配音缓存都有独立内容清单；完整缓存清单及强制退出后的跨文件恢复仍需继续审查。

定向回归包含相同大小 / 时间戳替换、原片与工作副本分别被修改、恢复校验间隙变化、缓存重烧回退、哈希取消、坏身份记录及术语变化。真实 FFmpeg 生成与烧录测试也验证：模拟识别结束后替换原片，仍可从有效工作副本完成视频输出。

真实 Windows CPU 验收使用中文音频、Whisper tiny 和 FFmpeg：识别得到 2 条字幕后，将原视频替换为另一份红色视频；输出取样为深蓝 RGB `[0, 0, 127]`，确认烧录使用原工作副本，报告内容摘要与原片一致。中文目标繁体场景未调用配音。证据保存于本地 `.verify/source-identity-live/acceptance.json`；该测试不是 Apple GPU 验收，也没有重新安装全部运行环境。

最终本地完整回归 **575 项通过、1 项跳过**，核心覆盖率 **90.08%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **78 个源码文件**通过。[GitHub Actions 33979166288](https://github.com/crazymsn/SubFlow/actions/runs/33979166288) 整体成功，Windows、Apple Silicon、Intel Mac 客户端与 Docker / Compose 检查通过。

## 1.3.13：并发文件占用与旧成片身份

- 流水线在处理前登记输入、参考音频、术语文件、输出、待复用成片和整个工作目录。写入与活动读写占用冲突时拒绝执行；只读输入可以共享。原工作目录锁保留。
- 登记采用短时间持有的协调锁和每次任务独立的操作系统所有权锁。不能用 PID、时间戳或旧文件推断任务仍在运行；所有权锁已释放才清理失效登记。登记锁等待支持取消，清理遇到锁忙时释放所有权，留下可回收记录；有效但损坏的登记阻止新任务冒险写入。
- 文件路径规范化，并检查现有硬链接身份；工作目录覆盖动态子路径，另列主要工作文件以识别其目录外硬链接。Mac 对大小写别名保守处理。任务配置和嵌套设置在入口复制，防止外部修改绕过登记范围。
- GUI 另存成品使用同一占用机制和原子复制，保护已有单个目标文件；所有预期源文件在开始复制前检查。它不承诺多份成品在 I/O 失败时整体回滚。
- 成片报告新增 `output_video_sha256`，校验完整文件内容并支持取消。外部成片与报告不符，或旧报告没有内容摘要时，不直接复制旧路径，而是使用已有字幕重新烧录；无须因此重跑识别。校验期间文件发生变化会报错。
- 默认登记位于当前用户缓存，Compose 使用已共享的缓存卷。跨进程保护要求相同登记目录、相同路径映射和正常文件锁语义；不约束未接入机制的外部程序。强制退出时多文件提交的一致性、完整工作缓存内容身份及独立下载入口的协调仍需继续审查。

已完成真实独立进程的冲突拒绝、共享读取、硬链接冲突和崩溃回收测试，以及目录包含关系、登记等待取消、损坏登记、GUI 冲突 / 缺失文件 / 复制失败和配置快照测试。旧成片测试用相同大小和时间戳的不同内容验证不能误复用，并覆盖旧报告无摘要的重建及哈希取消 / 变化检测。

本地完整回归 **557 项通过、1 项跳过**，核心覆盖率 **90.08%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **78 个源码文件**通过。[GitHub Actions 33978462919](https://github.com/crazymsn/SubFlow/actions/runs/33978462919) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包和启动检查，以及 Docker / Compose 检查通过。完整 Apple GPU 实机验收仍由用户最后在 M1 MacBook Air 上自行完成。

## 1.3.12：导出提交、取消复制与字幕恢复

- 成片、独立配音、缓存导出、工作源视频及下载提交前的复制均使用目标目录内的临时文件，按 1 MiB 分块响应暂停 / 停止。写入并同步完成后检查取消，再原子替换目标；复制失败不截断旧文件。
- 复制前后比较源文件大小、修改时间和文件标识，检测到变化时拒绝提交；这不是抵御刻意保持元数据不变的并发修改的内容哈希校验。源文件与目标为同一文件时不复制，缺失源文件正常报错。
- 导出的 ASS 和 SRT 先各自准备全部数据及原文件备份，再提交。可观察的替换错误触发逆序回滚；回滚也失败时保留备份，并在错误中列明恢复路径。ASS 仍使用 UTF-8 BOM，SRT 使用 UTF-8。
- 字幕对提交成功后再更新工作目录的 ASS；工作文件复制失败仍报告失败，已导出的字幕对保持一致。恢复后缺失 SRT、外部 ASS 或工作 ASS 时重新渲染，不再只复制 ASS 后声称 SRT 已完成。
- 临时复制文件可能继承 Windows 只读属性，清理时仅对本次拥有的临时文件恢复写权限；回滚失败时明确保留的备份不清理。
- 这里保证单个媒体文件原子替换及字幕组在常规 I/O 错误时回滚，不宣称跨文件系统的整任务原子事务；进程在多文件提交途中被强制终止、多个不同任务并发写相同输出的整体一致性仍需继续审查。

定向验证覆盖复制中途取消、同步 / 替换失败、源文件变化、只读清理、硬链接别名、第二份字幕准备或提交失败、回滚失败保留备份、首次目录创建、编码、恢复缺失字幕及缓存成片复制取消。故障由测试钩子注入，不冒充真实磁盘耗尽或硬件故障。

本地完整回归 **542 项通过、1 项跳过**，核心覆盖率 **89.79%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **77 个源码文件**通过。完整回归也包含实际 FFmpeg 字幕烧录、失败恢复和缓存导出。[GitHub Actions 33977709974](https://github.com/crazymsn/SubFlow/actions/runs/33977709974) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包和启动检查，以及 Docker / Compose 检查通过。

## 1.3.11：单行字幕文本、时序与配音句子

- 单行模式使用与渲染相同的语言选择规则。识别词条只有在完整文字对应、时间有限且有序、位于原句区间内时才提供拆分边界；不再用源语言词条重建译文。中文拆分不插入词间空格。
- 迭代拆分处理每个剩余长片段，保留全部非空白字符和原始起止时间，拆分内部边界相接；不延长短片段覆盖下一句，不裁掉长片段的末尾。空白归一化为单行，组合重音和 ZWJ 连接的表情不从连接位置切开。
- 拆分结果仅保存当前显示文字，完整双语字幕另行保留。单行模式配音从完整双语字幕读取句子及时间，首次处理和直接恢复到配音阶段使用同一路径，避免重复整句或将排版片段当成独立配音句。
- `report.json` 的 `subtitle_fit_warnings` 记录未满足的行长、阅读速度、最短和最长时长，缓存导出重新计算同样的检查。文本和可用时间同时拆分不能提高整体阅读速度；过短区间、不可再拆的长时间单词保留内容和时间并报告实际限制。本项目这些阈值是排版规则，不作为 Netflix 官方交付认证。
- 处理缓存修订已更新，旧版本的字幕和成片需重新处理。

回归包含中英双向、简繁中文、德文、源文本选择、完整 / 缺失 / 无效词对齐、极短时间、超长文本、阅读速度和组合字符。集成测试实际生成并烧录短视频，模拟识别、翻译及配音响应；验证配音失败后的恢复、完整句子传入、缓存导出、报告一致性及源视频不变。这里不将模拟配音响应计为真实语音合成验收。

本地完整回归 **521 项通过、1 项跳过**，核心覆盖率 **89.09%**。随后增加简体 / 繁体两项流程回归，连同烧录与恢复测试共 **3 项通过**：确认中文源片即使启用配音也不调用翻译或语音合成，字幕采用所选字形。Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **76 个源码文件**通过。[GitHub Actions 33977237072](https://github.com/crazymsn/SubFlow/actions/runs/33977237072) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包和启动检查，以及 Docker / Compose 检查通过。M1 MacBook Air 最终实机验收由用户自行执行。

## 1.3.10：完成记录、恢复阶段与缓存损坏

- 新尝试先写入新的任务编号和初始完成阶段，恢复也分配新编号。完成报告必须与当前任务编号一致，防止旧报告在新任务失败后驱动缓存导出。
- 恢复入口核对前置阶段已完成；从较早阶段重新运行之前，将完成记录退回其前一阶段。取消记录保留最后完成阶段，不能从失败步骤之后继续。未知恢复阶段在创建工作目录前直接报错。
- 状态、输入记录、最终报告、最近任务指针和字幕 JSON 均先在同目录临时文件写入、同步，再原子替换。此处保证单个记录的提交；并未宣称多个媒体和字幕文件组成一个原子事务。
- 字幕缓存要求列表及合法条目，校验文本、词条、有限非负时间和正时长；损坏的已完成字幕缓存触发重新处理。很短的词级字幕至少保留一个厘秒，避免舍入为零时长后无法保存。
- 缓存导出检查当前任务编号、完成阶段、画面尺寸、计数、时长、布尔标志及字幕数量。检查在写出文件之前完成；有效旧状态格式可读取，但缺失完成阶段记录时不允许跨阶段恢复。
- 被选中重跑的静音步骤重新检测，恢复读取时校验静音区间。真实 FFmpeg 输入错误正常抛出，不再把检测失败保存为空列表。
- 处理配置修订已更新，使升级前的旧处理缓存失效；旧缓存无法直接跨过识别阶段恢复，需要从 ingest 重新处理。

真实 Windows CPU 验收使用合成短视频和 Whisper tiny：首次得到 2 条字幕，模拟识别中断后旧转写保持原样，跳过失败识别的恢复被拒绝，重新识别后再次得到 2 条字幕；源视频哈希保持不变。该验收包含实际 FFmpeg 音频提取和静音检测，识别失败由测试钩子注入，不冒充真实模型故障。本地完整回归 **499 项通过、1 项跳过**，核心覆盖率 **88.01%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **76 个源码文件**通过。[GitHub Actions 33976662578](https://github.com/crazymsn/SubFlow/actions/runs/33976662578) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包和启动检查，以及 Docker / Compose 检查通过。

## 1.3.9：识别结果完整性与字幕缺失

- 处理配置记录新增算法修订，升级后不复用旧缺字字幕和成片；旧缓存不能跨过识别阶段继续恢复，需重新处理一次。

- 发现并修复混合词级对齐时丢字幕：原逻辑只要找到任意词级对齐，就跳过其他句子。现在按连续的可靠对齐区域构建字幕，不可靠区域使用完整句子及句级时间；保留相邻可靠区域的精细时间。
- 对齐文本必须完整覆盖句子，词时间必须有限、有序并位于句子内；缺失时间不再扩展成整句时间。可选对齐失效时保留句子，缺失 segments、非文本内容、非有限 / 负数 / 倒序时间等识别主结构错误明确失败。
- Whisper、WhisperX 和缓存读取共用标准库实现的校验格式。写结果先完成校验，再写同目录临时文件、同步并原子替换，失败保留原文件。
- 外部工作进程输出到每次独立目录。必须确认本次输出存在且有效，检查取消后才提交；防止工作进程退出码为零但未生成文件时误读上一轮识别结果。
- 外部识别共用进程树管理，异常退出与取消均清理后代；日志读取限制尾部。源码进程内识别在推理结束后检查取消再提交，该路径的模型计算本身仍不是即时可取消。
- SRT / ASS 先按毫秒 / 厘秒舍入总时间，再进位分解；验证 59.9996 秒和 3599.9996 秒等边界，拒绝负数和非有限时间。
- 打包包含共享校验模块，并实际用外部 Python 执行两份随包工作脚本的参数入口，避免模块仅存在于 PyInstaller 内部归档而工作进程无法导入。

本地完整回归 **468 项通过、1 项跳过**，核心覆盖率 **87.86%**；真实 Windows CPU Whisper tiny 识别中文音频得到 2 段有效结果，并确认临时目录清理。Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **75 个源码文件**通过。本地 Windows 随包两份 ASR 脚本均通过外部 Python 启动检查，随包 Whisper 完成真实中文 CPU 识别（2 段）；脚本和共享模块与最终源码哈希相同。冻结版阻塞 HTTP 下载取消、旧视频保护也通过。最后的缓存算法修订已通过源码回归。[GitHub Actions 33976058661](https://github.com/crazymsn/SubFlow/actions/runs/33976058661) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包与启动检查，以及 Docker / Compose 检查通过。完整模型的 WhisperX 实机识别仍需另行验收，本轮不以模拟工作进程替代其模型验证。

## 1.3.8：配音服务生命周期

- 服务使用与下载及安装器相同的受管理进程树。Windows 使用 Job Object，Mac/Linux 使用独立进程组；服务父进程先退出后仍清理后代，启动失败移除旧服务记录。
- `start_server` 直接启动的服务也登记资源所有权，统一由 `stop_servers` 清理。通过健康检查发现的外部服务不会登记或终止。
- 依赖与 TTS 导入探测使用临时文件记录输出，避免父进程退出但子进程继承管道导致等待 EOF；每次探测 90 秒超时，支持任务停止及应用退出信号，异常路径清理后代。
- 等待另一个启动调用释放锁期间支持取消。共享配音服务的就绪等待超时后仍可继续加载，取消单个等待者不主动杀死其他任务共享的服务；应用退出统一清理。
- 服务启动使用统一隔离环境，移除宿主 Python / venv 环境干扰；故障日志最多读取最后 64 KiB，再截取需要展示的行。
- 新增真实进程测试：正常服务退出、父进程崩溃、失败启动回收、依赖探测崩溃 / 超时 / 取消、等待启动锁时取消、启动途中关闭清理尚未登记的服务，以及外部进程不被关闭。

本地完整回归 **433 项通过、1 项跳过**，核心覆盖率 **87.83%**；Ruff 与 mypy 的 **74 个源码文件**检查通过。Windows 实际启动现有 GPT-SoVITS 完整环境并完成英文合成，生成可读取的 **6.0 秒 WAV**；服务报告 `device=cpu`、`is_half=false`、`torch_version=2.5.1+cu124`。关闭后资源登记为空，并在 5 秒等待上限内确认所有已观察后代退出。该检查使用现有解释器和模型，没有重新进行冷安装，也不作为 Apple GPU 证据。[GitHub Actions 33975269813](https://github.com/crazymsn/SubFlow/actions/runs/33975269813) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包、启动检查，以及 Docker / Compose 检查通过。

## 1.3.7：托管环境修复与资源完整性

- 复用托管环境前执行真实依赖导入检查。检查失败时重建现有 venv 启动文件、强制重装依赖；只有最终检查通过才原子写入就绪标记。取消检查不误触发重装；失败后重试不误用旧标记。
- 模型与语言包固定上游 revision，按远程 Git blob SHA-1 / LFS SHA-256 验证下载缓存，校验失败强制重新下载一次。复制到目标后再次验证内容，再原子替换该文件；错误不提交该文件。
- 语言压缩包先在私有目录完整解压，CRC 失败不触碰现有文件；检查路径越界和符号链接。提交为逐文件替换，不宣称整个多文件目录具备事务回滚。
- 保存全部已安装模型和解压资源清单，覆盖 NLTK 与 fast_langdetect 等数据。正常启动检查文件存在、大小、修改时间；修复安装执行内容校验，`assets_ready(full=True)` 可执行全部文件哈希。正常启动不检测刻意保持相同大小和修改时间的损坏。
- 安装进程使用受管理的进程树，检查超过 90 秒按超时处理；实际父子进程测试覆盖安装器异常退出和检查超时后的清理。
- `SUBFLOW_AUTO_INSTALL=0` 时缺失源码或资源直接报告原因，不再偷偷复制或下载；显式自定义运行目录保持已有选择逻辑。
- 真实验证 uv 能修复损坏的 venv 启动器；真实下载固定版本 Hubert 配置及 fast_langdetect 小模型并通过远程摘要验证。本轮没有重新下载全部多 GB 模型，其他损坏资源和压缩包边界由隔离回归验证。
- 用户使用 M1 MacBook Air，要求最后自行测试；已提供 [无开发环境要求的实机步骤](mac-self-test.md)，包含实际设备前后查询、原声保护、跨语言配音、日志保存与 CPU 运行。

本地完整回归 **424 项通过、1 项跳过**，核心覆盖率 **87.83%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **74 个源码文件**通过。现有 Windows 托管 GPT-SoVITS 解释器实际完成扩展依赖导入检查。[GitHub Actions 33974776630](https://github.com/crazymsn/SubFlow/actions/runs/33974776630) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包和启动检查，以及 Docker / Compose 检查通过。

## 1.3.6：下载工作进程与桌面任务控制

- 客户端使用独立下载工作进程，进度和结果由本次临时目录传递，只有父进程负责最终提交视频。停止时终止进程，不再依赖阻塞网络调用返回进度回调。
- Windows 在工作进程恢复运行前将其放入 Job Object，关闭该对象时终止所有后代；Mac/Linux 为工作进程创建独立会话，内部自有 FFmpeg 保留在同一进程组，正常结束和异常退出均清理后代。
- Windows 暂停先停止父进程，再递归暂停子进程；恢复覆盖整棵进程树。避免只挂起 venv 启动器，而实际 Python / FFmpeg 仍继续工作。
- 桌面单独下载使用与处理任务相同的暂停、恢复、停止按钮；二者互斥，下载线程结束信号处理完才释放控制权。取消及关闭客户端时不再弹出下载失败对话框。
- 源码实际测试覆盖阻塞 HTTP 请求在 10 秒内取消、父子进程计数在暂停时停止并在恢复后继续、停止已暂停任务，以及工作进程异常退出后无孤儿进程。此前真实 HTTP 传输测试已切换为独立工作进程执行。
- 打包入口在加载 Qt 界面前分派下载任务。发布检查验证该入口能返回结构化错误，并用打包程序向本地 HTTP 服务发起实际阻塞请求，检查取消后保留旧视频且清理临时目录。

本地完整回归 **408 项通过、1 项跳过**，核心覆盖率 **87.83%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 **74 个源码文件**通过。Windows 本地已完成打包、入口检查及冻结版实际 HTTP 阻塞取消，旧视频保持完整且临时目录已清理；[GitHub Actions 33974006475](https://github.com/crazymsn/SubFlow/actions/runs/33974006475) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包和启动检查，以及 Docker / Compose 检查通过。

## 1.3.5：下载提交、取消及路径约束

- 下载改为在目标目录下独立临时目录准备，成功后原子替换 `source.mp4`。不再清理用户目录中的 `source.*`、其他任务的 `.part`；同一目标目录由文件锁保护。
- 每次备用下载尝试清理本次私有目录内的旧媒体，避免失败尝试残留文件被下一次错误地认定为成功。
- 取消校验覆盖格式查询重试、下载前后、进度、后处理回调、转封装以及复制到工作目录之后。暂停的下载进度回调等待恢复；格式查询中的取消不再被通用异常处理吞掉。
- 转封装使用临时 MP4，支持任务控制，成功后替换原有输出；失败或取消保留旧文件，不再返回随后被删除的 `.best` 文件路径。
- 下载身份记录包含链接、工作副本路径、大小和修改时间。文件被替换后重新下载；缺少记录的旧缓存不能仅凭链接文本判定有效。现有显式恢复任务仍按任务输入记录验证。
- URL 派生目录名限制为安全、有限长度的单个目录组件，防止路径越界和 Windows 保留名称错误。YouTube / Bilibili 识别检查真实主机名，避免查询字符串、用户名或仿冒域名误匹配；不再关闭 HTTPS 证书验证。
- 真实本地 HTTP 视频传输、成片媒体检查和不兼容音频的 FFmpeg 转封装已验证。本地 HTTP 不提供平台格式表，该测试只替换格式选择，不替换网络下载、回调或探测；YouTube / Bilibili 格式选择仍由专门回归覆盖。

边界：当前仍使用 yt-dlp 进程内 API，同步网络调用与 yt-dlp 内部 FFmpeg 的执行期间不能保证立即暂停/取消，需等待回调或超时；自有转封装进程已接入任务控制。此轮没有宣称完成下载器全进程树的端到端取消验收。

本地最终完整回归 **404 项通过、1 项跳过**，核心覆盖率 **86.83%**；Ruff 通过，按主机 Python 3.13 参数运行的 mypy 检查 72 个源码文件通过。[GitHub Actions 33973419384](https://github.com/crazymsn/SubFlow/actions/runs/33973419384) 整体成功，三个桌面平台的测试、依赖安装、打包与启动检查，以及 Docker / Compose 检查通过。

## 1.3.4：字幕烧录与翻译网络异常

- 真实复现单引号、中文、括号、分号及逗号路径造成 FFmpeg 字幕过滤器解析失败。字幕与字体改用临时目录内的固定相对名，用户路径保留为独立进程参数；真实成片解码帧中的白色字幕像素验证已实际烧录。
- 烧录先写目标同目录临时文件，成功后原子替换。编码失败、取消均清理临时成片并保留已有输出。
- 401 / 403 鉴权错误及永久请求错误不再触发逐句重试；429、指定 5xx 和连接失败最多请求四次，关闭 SDK 内部重复重试。
- 只有明确不支持 JSON 格式的 400 / 422 响应才尝试普通格式；鉴权、服务错误及取消正常向上传播，不被润色或词汇表的降级处理吞掉。
- 退避等待支持暂停和停止。请求设置 60 秒网络超时；同步 HTTP 执行期间仍需等待当前请求返回或超时，并非即时取消或严格的总耗时上限。
- 普通翻译缓存纳入字符限制、语种和词汇表，润色缓存纳入词汇表。新缓存键不会复用缺少这些条件的旧缓存，因此升级后可能重新请求一次。
- 翻译行数及类型验证覆盖空响应；润色不再将 null / 对象转成字幕。仅完整连续编号才移除数字前缀，保留年份和小数。

新增实际编码及文件完整性、网络故障、错误传播、缓存隔离、数字文本和取消回归。本地完整测试 **370 项通过、1 项跳过**，核心覆盖率 **86.61%**；`ruff check src tests scripts` 通过。当前主机 Python 3.13 环境使用 `mypy src/bilingual_sub --ignore-missing-imports --python-version 3.13`，72 个源码文件通过；该检查不包含第三方未提供的类型声明。[GitHub Actions 33972765079](https://github.com/crazymsn/SubFlow/actions/runs/33972765079) 整体成功，三个桌面平台的测试、依赖安装、打包、启动检查和 Docker / Compose 检查全部通过。

## 1.3.3：缓存配置与任务生命周期

- 分别保存实际处理配置和渲染配置。识别、静音、断句、翻译、预览或词汇表变化时不复用旧任务；样式文件内容或烧录质量变化时重新渲染原声成片，不重新识别。只有输出路径变化时仍可复用。
- 恢复任务同样验证处理配置。旧缓存缺少配置记录时，需要从 ingest 重新处理一次，不能凭未记录的默认值认定它与当前配置相同。
- GUI 快速复制路径纳入视频大小/修改时间、参考音频内容、配置和样式内容，使用开始处理时的签名，避免完成时误记变更后的配置。
- 工作目录持有系统文件锁，同一目录同时只能有一个任务写入；失败和取消均释放锁。
- Whisper / WhisperX 统一等待与清理逻辑，正常结束解除绑定，回调错误或取消时终止仍在运行的工作进程。
- Mac/Linux 子进程使用独立会话，暂停、恢复、终止仅针对本任务进程组；Windows 继续使用已有进程树终止方式。
- 暂停、恢复和进程绑定串行更新状态，避免恢复操作抢在挂起之前执行。管道读取失败不再伪装为成功，异常路径也清理子进程。

本地完整回归 **329 项通过、1 项跳过**，核心覆盖率 **85.95%**；Ruff 和 mypy 的 **72 个源码文件**检查通过。新增实际父子进程、异常清理、暂停恢复并发、目录互斥，以及样式/编码配置变更回归。[GitHub Actions 33972067511](https://github.com/crazymsn/SubFlow/actions/runs/33972067511) 整体成功，Windows、Apple Silicon、Intel Mac 的依赖安装、测试、打包及启动检查和 Docker / Compose 检查通过。

## 1.3.2：输入、恢复与导出一致性

本轮检查了 pipeline、输出路径、GUI 重新导出、下载工作副本与恢复任务相关代码，并补充失败场景回归：

- 同样大小的另一部视频曾被误判为同一任务；现在校验原路径、大小、修改时间，并将验证应用于显式指定的恢复目录。
- 新任务在识别、翻译之前保存 `job_input.json`，翻译中断后仍能验证任务身份并复用已完成的识别。没有可核对身份的旧任务不能仅凭文件大小恢复。
- 普通新任务缺少输入视频时，明确报错，不会悄悄选用工作目录内另一部旧视频。
- 新链接在独立下载目录准备，成功后再替换 `source.mp4` 及链接记录。失败重试不会把旧视频关联到新链接。
- 写入前统一检查视频、SRT、ASS 及配音输出冲突，保护原视频、参考音频、词汇表和中间数据。相同路径、符号链接和已有硬链接均识别为同一文件。
- GUI 重新导出也保护原视频；无烧录任务不会把目标目录中已有的无关 MP4 当成自己的成品。
- 重新导出后同步结果对象和报告中的实际配音路径，连续迁移导出目录仍返回正确位置。
- 已停止的任务不进入缓存导出，重新导出完成前再次检查停止状态。
- 自动生成词汇表的开关纳入缓存身份，避免开关变化后复用不同处理配置的结果。

1.3.2 的 [GitHub Actions 33971384532](https://github.com/crazymsn/SubFlow/actions/runs/33971384532) 整体成功：三个桌面平台各 **321 项通过、1 项跳过**，依赖安装、打包、启动检查及 Docker / Compose 检查通过。本地核心覆盖率 85.05%，Ruff 及 mypy 的 71 个源码文件检查通过。

## 已有验收证据

- [初始中文配音和 Windows 长命令修复](qa-2026-09-05.md)：真实中文短视频、批量混音和随包配音服务。
- [自动环境与社区部署](community-qa-2026-09-05.md)：Windows / Mac / Docker 构建，以及 Windows 冷启动、实际 CPU 识别和配音。
- [Apple GPU 实现与边界](apple-gpu-qa-2026-09-05.md)：原生依赖安装成功，托管 Mac 虚拟机不具备可用于验收的 GPU。

## 继续审查的范围

1. 继续检查任务状态持久化、处理中外部输入文件变动及各条恢复路径；已有缓存、目录锁和进程回归不等于覆盖所有异常时序。
2. 下载器内部启动的进程、网络中断及资源安装进程的端到端取消行为。
3. 网络接口、翻译缓存、模型及语言资源下载的异常恢复和输入校验。
4. ASR、字幕时间轴、Netflix 单行拆分与译文对应、渲染、配音模型及 GUI 全部交互路径的交叉验证；核心覆盖率不能代表 GUI 或第三方源码的全量覆盖。
5. vendored GPT-SoVITS 全部源码及依赖的系统检查；已有验收主要覆盖 SubFlow 默认推理路径，没有完成上游全部训练、模型变体的测试。
6. 实体 Apple M 系列 GPU 的识别、配音、内存和性能验收。用户已有 M1 MacBook Air，明确选择最终自行测试；已提供独立验收清单，实测结果待用户确认。

整体目标仍在进行中。
