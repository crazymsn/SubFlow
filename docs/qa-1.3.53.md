# 1.3.53 本地验收记录

> 历史记录：此文件保留当时的结果和版本状态，不代表当前安装包。最新使用说明见[文档索引](README.md)。

日期：2026-09-06。设备：Windows，RTX 3060 Laptop GPU（6 GB）。本次未发布 GitHub / Docker Hub，也未进行 Mac 实机验收。

## 完成内容

- 9 个官方预设 + 14 个独立设计音色，共 23 个（12 男声、11 女声）。七种口语覆盖八种字幕目标，中文简繁共享普通话声音。
- 设计音色由官方 1.7B VoiceDesign 在本机 CUDA 上生成，客户端复用既有 0.6B Base，无新增首次下载。生成描述、参考文字、固定模型 revision 和实际 WAV 摘要保存在产品音色清单。
- 语音识别与翻译常显；移除折叠控件、回调和失效样式，更新八种界面语言及说明截图。

## 真实合成

所有下表检查均使用完整包内的 Python 和模型、空白用户缓存，禁止外网连接与安装子进程，禁止推理进程写入模型包。

| 检查 | 实际设备 | 合成结果 |
| --- | --- | --- |
| qwen-native | CPU | PCM WAV，4.56 秒，非静音 |
| qwen-clone | CPU | PCM WAV，4.48 秒，非静音 |
| gptsovits | CPU | PCM WAV，4.46 秒，非静音 |
| 新增设计音色（14 个） | CUDA | 全部成功，1.52–4.80 秒 |
| SubFlow 法语设计女声 | CPU | 2.16 秒；设计音色与官方预设双向切换通过 |
| 官方预设 ↔ 设计音色 | CUDA | 切换释放旧模型，服务 revision 保持一致，未触发 CPU 回退 |

GPT-SoVITS 清理后仍完成中文、英语、日语、韩语、粤语五套前端词典检查及真实 CPU 合成。保留当前使用的 v2 配对权重和全部必要字典。

## 内容检查

使用本机 Whisper medium（CUDA）分别转写 14 段产品参考音频与 14 段实际试听输出。统一 Unicode、标点、空格及中文简繁之后，参考文本字符相似度为 98.7%–100%，试听文本为 100%。该指标用于检查错漏字，不等于母语口音、情绪或语调评分。性别与口音目标由音色设计描述指定，最终听感仍需用户试听。

| 音色 ID | 参考音频时长 | GPU 试听时长 |
| --- | ---: | ---: |
| SubFlow_zh_female | 6.08 秒 | 2.72 秒 |
| SubFlow_zh_male | 5.68 秒 | 2.56 秒 |
| SubFlow_en_female | 6.08 秒 | 2.16 秒 |
| SubFlow_en_male | 5.36 秒 | 1.68 秒 |
| SubFlow_ja_female | 7.76 秒 | 4.16 秒 |
| SubFlow_ja_male | 6.32 秒 | 4.80 秒 |
| SubFlow_es_female | 6.56 秒 | 2.32 秒 |
| SubFlow_es_male | 5.92 秒 | 1.52 秒 |
| SubFlow_fr_female | 5.84 秒 | 2.56 秒 |
| SubFlow_fr_male | 8.08 秒 | 2.24 秒 |
| SubFlow_de_female | 7.12 秒 | 2.56 秒 |
| SubFlow_de_male | 7.28 秒 | 2.88 秒 |
| SubFlow_ru_female | 6.32 秒 | 2.40 秒 |
| SubFlow_ru_male | 6.88 秒 | 2.48 秒 |

## 代码与客户端

- 八种界面语言 × 四种窗口尺寸布局验证通过，并人工查看真实 Qt 深色、浅色和德语小窗口截图。常显设置可滚动访问，底部操作按钮没有被遮挡。
- 初轮相关检查 108 项通过，1 项因设计音频尚在生成而未找到清单；资源生成后复检音色与服务相关 50 项全部通过。另执行环境、离线资源及服务契约检查 40 项全部通过。这些批次有重叠，不相加计数。
- Ruff 对本轮修改的 Python 文件检查通过。
- 打包目录和原目录的实际 EXE 自检均通过，覆盖音色清单及全部参考文件哈希、界面、媒体工具、识别 worker 和下载 worker。
- 更新原目录 27 个文件，逐一核对 SHA-256；配置、Cookies、FFmpeg / ffprobe 和原模型清单保持不变。更新了随包第三方声明。
- 原路径冻结客户端：空白用户配置、自动安装关闭，自动发现随包环境和模型，在 `cuda:0` 合成法语设计女声 2.56 秒；关闭客户端后推理子进程退出。
- 原路径 EXE SHA-256：`eea5a2cd2e98a2aaa07020721b5adb315aa380e01cf825397e548f01b68d0f40`。

## 清理范围

- 删除旧完整包及临时构建目录、被离线包替代的 `dist/SubFlow/GPT-SoVITS` 副本、未在使用的 v1 / v3 / v4 / v2Pro 原版权重和对应缓存。
- 首轮删除清单的逻辑文件总量约 25.86 GiB；部分文件是硬链接副本，因此不能把该数字等同于实际释放磁盘空间。
- 项目内打包中间目录、缓存和测试产物已清理；正式文档截图、产品音色 WAV 和验证记录保留。
- 系统 TEMP 目录中的本轮临时 VoiceDesign 权重（约 4.21 GiB）及验收音频/日志尚未删除：批量删除和缩小到单个模型目录的删除均被自动审批拦截，工具仅返回 `blocked by policy`，未给出进一步理由。它们位于项目外，不包含在客户端中。
- 不删除用户视频、成片、配置、正式模型、在用 Python 环境、语音字典或回归测试源码。清理前检查绝对路径边界、运行进程、保存的引用；清理后核对客户端与配置摘要。

## 尚未实机覆盖

M1 MacBook Air 的 MPS 推理、显示及性能由用户自行验收。本机检查无法替代真实 Mac。较新的 Qwen-Audio-3.0 公开报告不代表存在可内置的开源权重；模型选择依据及官方来源见 [音色与设备](voices.md)。
