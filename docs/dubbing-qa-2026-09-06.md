# 配音恢复验收（2026-09-06）

> 历史记录：此文件保留当时的结果和版本状态，不代表当前安装包。最新使用说明见[文档索引](README.md)。

适用范围：本地源码及 Windows 1.3.47 客户端；本次未发布 GitHub Release 或 Docker 镜像。

## 恢复与修复

- 恢复被移除的配音模块、界面、CLI、自动安装资源和桌面 / Docker 打包配置。
- 恢复 GPT-SoVITS 与 Qwen3-TTS 自动语言路由、逐句进度、GPU 优先及 CPU 后备路径。
- 自动提取的参考音频不再搭配不相符的保存逐字稿；明确留空的逐字稿不会被环境变量覆盖。
- 配音从环境准备到音轨验证全程持有引擎使用锁，防止同客户端的试听切换引擎打断配音。
- 失效参考文件在加载模型前报错；修复默认截去开头 0.4 秒导致合格短参考音频不足 3 秒的问题。
- 修复试听时显式指定的 Qwen 服务地址被默认地址覆盖，并使用对应引擎的健康检查。

## 自动化检查

| 检查 | 结果 |
| --- | --- |
| 全量回归 | 1,373 通过，3 跳过；核心代码覆盖率 91.84% |
| 新增问题与流水线定向检查 | 59 通过 |
| 最终配音、参考缓存、GPU 选择及服务契约检查 | 161 通过 |
| Ruff / Git 差异空白检查 | 通过 |
| 新客户端及原路径客户端自检 | 八种目标路由、进度文案、错误弹窗、安装资源、外部识别进程、下载取消均通过 |

全量回归后补充的修复由定向检查覆盖。测试使用隔离配置与合成数据，未修改日常用户配置。依赖库有弃用提示，不影响本次测试结果。

## 真实推理与音频处理

设备：Windows，NVIDIA GeForce RTX 3060 Laptop GPU（6 GB），PyTorch 2.5.1+cu124。参考音频由 Windows 中文语音生成，未使用用户视频或真实测试素材。

| 引擎 | 目标 | 推理设备 | 合成音频时长 |
| --- | --- | --- | --- |
| GPT-SoVITS | 简体中文 / 繁体中文 / 英文 / 日文 | CUDA | 2.82 / 2.70 / 2.08 / 3.56 秒 |
| Qwen3-TTS | 西班牙文 / 俄文 / 法文 / 德文 | CUDA | 2.32 / 4.72 / 3.76 / 2.80 秒 |
| Qwen3-TTS | 法文短句 | CPU（隐藏 GPU） | 1.04 秒 |

Whisper small 对生成音频再次识别，确认各语言输出包含预期问候语；这是辅助内容检查，不代表声学质量或逐字准确率评测。

实际完成两句英文合成、时间对齐及 8 秒视频封装；原视频摘要保持不变。另完成 150 段长路径音频混音，原始参数总长度超过 Windows 单命令限制，分批处理成功且没有 WinError 206。

中文源片到简体 / 繁体中文的视频任务由流水线回归验证保留原声。中文独立试听仍可生成中文问候语。

## 本地交付与边界

原路径 `dist/SubFlow/SubFlow.exe` 已更新到 1.3.47，更新的 5 个文件逐一校验摘要。已有 GPT-SoVITS 模型、CPU / CUDA 环境和用户配置保留。

客户端 SHA-256：`FC6225FF835D2CE700BC9C39736192CF19E15953958CA4095F1C6296435074F1`。

Apple MPS 选择与后备行为经过代码回归；真实 M1 MacBook Air 验收仍由使用者进行。本次没有执行新的 macOS 构建或 Docker 镜像构建。
